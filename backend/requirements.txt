# Kylin SafeOps Agent — 后端运行时依赖
# 版本策略：核心运行时包 == 锁定，与麒麟 V11 靶机 Python 3.11.6 + CI(3.11) + 本地三者一致，
# 可复现、杜绝"本地绿 CI 红"漂移。
# dev / CI 工具（ruff/mypy/pytest/pre-commit）已拆分至 requirements-dev.txt。
# LoongArch 铁律：运行时唯一编译型依赖为 pydantic-core（Rust）→ 需 VM 预编译 wheel 并记部署文档。
#   （L-1 整改：移除 mcp 运行时依赖——mcp 会传递拉入 jsonschema→rpds-py(Rust)、
#    pyjwt[crypto]→cryptography→cffi(C) 等编译件，与上述铁律冲突，且全仓仅用一个 Tool 类型，
#    已改为 backend/app/mcp/protocol.py 自写薄 Tool 类型。）

# --- core / contracts ---
pydantic==2.13.4       # 契约模型；pydantic-core 为 Rust 扩展 → LoongArch 需预编译 wheel

# --- api / orchestration ---
fastapi==0.136.3
uvicorn==0.49.0        # 纯 uvicorn，不加 [standard]（避 uvloop/httptools C 扩展上 LoongArch）

# --- llm gateway ---
httpx==0.28.1          # OpenAI 兼容网关客户端（纯 Python）

# --- MCP 协议层：自写薄 Tool 类型（protocol.py），不依赖官方 mcp SDK（见上 L-1 整改说明）---
#   fastmcp / mcp 源码仅留 third_party 作参考，不进运行时。

# --- 反代 SSO 真 LDAP（deploy/sso/ldap_client.py）---
ldap3==2.9.1            # 纯 Python；<3.0 锁版本防传递依赖漂移（LoongArch 移植风险低）
                        # pyasn1==0.6.3 由 ldap3 拉入，进 constraints.txt 锁死
