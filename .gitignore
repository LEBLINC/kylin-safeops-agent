# --- 参考克隆（永不提交，约 1.8GB） ---
_reference/

# --- 规划/策略/协作痕迹文档不入库；根级 .md 仅 README 例外 ---
/*.md
!/README.md
# 2026-07-16 之七十三收敛（撤销 2026-06-19 的 4 文档入库例外）：4 份对齐文档退库
# （git rm --cached，本地保留协作留痕）；docs/（ADR/设计稿/过程报告）整树退库。
#
# 2026-07-30 之七十五 H-8：交付面文档改白名单放行——ADR 是架构决策的权威记录、
# docs/v2 六份是对外交付物，二者必须随代码版本化（此前整树忽略导致 ADR 改动无法入库）。
# 内部过程稿（_planning / evidence / FACTS / GAPS / MERGE_PLAN 等）保持忽略。
# 注：git 无法 re-include 被排除目录下的文件，故不能写 /docs/ 后再 ! 放行，
# 必须逐层 /docs/* → !目录 → /目录/* → !具体文件。
/docs/*
!/docs/ADR/
/docs/ADR/*
!/docs/ADR/*.md
!/docs/v2/
/docs/v2/*
!/docs/v2/软件*.md
# 交付文档内嵌的全部 21 张图（六份 docs/v2 文档共引用 21 处 + README 2 张，
# 均落在这批里）。此前只放行 README 那 2 张，导致源码包内六份交付文档满屏破图。
# 仅放行 .png 成品：figures/src/ 下的 .mmd 源与字体（42 个文件）仍不入库——
# 它们是重新生成图片用的中间产物，读者不需要。
!/docs/figures/
/docs/figures/*
!/docs/figures/*.png
# 交付文档内嵌的实测截图（六份 docs/v2 文档共引用 56 处，全部落在本目录）。
# 此前整批被忽略，导致《功能测试报告》《性能测试报告》里的 VM 验证与性能证据
# 在源码包内全为破图——而这些恰是最需要随包留存的实测证据。
# 目录内 61 张中 5 张未被任何文档引用（改名前的重复副本 + 一版备选打码图），
# 由 F-2 用例钉住"未引用者不入库"，避免无出处的图混进交付面。
!/docs/screenshots/
/docs/screenshots/*
!/docs/screenshots/*.png
# 未被任何交付文档引用的 5 张，逐条排除（不是"漏引用"，已逐张核实）：
# 3 张与被引用图 md5 完全相同，是改名前的旧文件名副本；
# 09-rca（100%）与 acc-09-redacted 内容不同，是同一页面的另一次截图/另一版打码，
# 文档已各自选定其中一版，另一版不入交付面。
/docs/screenshots/09-rca（100%）.png
/docs/screenshots/09-rca（全部内容显示）.png
/docs/screenshots/14-line2-approval（R2）.png
/docs/screenshots/14-line2-approval（R3）.png
/docs/screenshots/acc-09-llm-health-redacted.png
# 竞赛交付文档（根级 软件*系列）经 /*.md 屏蔽，终稿以比赛提交包为准，不走本仓。
/文档_GPT/
/文档_claude/
/*.zip
scripts/check_docs_numbering.py

# --- 运行时产物目录 ---
/data/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.env

# --- 工具缓存 ---
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# --- 前端（x86 build，不在本仓追踪产物） ---
.npm-tmp-cache/
node_modules/
frontend/dist/
frontend/.vite/

# --- 数据 / 运行时 ---
*.db
*.sqlite*
*.log

# --- 编辑器 / OS ---
.idea/
.vscode/
.DS_Store
Thumbs.db
