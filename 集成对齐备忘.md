# 集成对齐备忘（审阅窗口维护，根级 md 不入库）

> 用途：把跨窗口（L / D / X）的关键决策、接口契约、集成切换点、开放项集中留痕，防遗忘与漂移。
> 维护者：审阅窗口。每轮有新决策即追加。最后更新：见末尾。

---

## 0. 当前状态快照

- 分支：`main`=稳定线 / `dev`=集成线（活跃）。远端 = **GitHub**（`LEBLINC/kylin-safeops-agent`，私有）。
  GitLab 已弃用（免费版私库 CI/CD 受限）；本地无 gitlab remote。
- `origin/dev` tip = `bac5e5b`（架构审计 L 侧整改 L-1~L-9 已合入：L-6/L-8 暂缓；含 D 的 PR1 evaluate + L1/L2/L3）。
- CI = GitHub Actions（`.github/workflows/ci.yml`），Python 3.11，四道闸：ruff / ruff-format / mypy(仅 contracts) / pytest。
- 仓库里还有一份 `.gitlab-ci.yml`（GitLab 弃用后的死配置，GitHub 忽略；留删由 L 定）。

### 真实 vs 注入桩
| 组件 | 归属 | 状态 |
|---|---|---|
| contracts(6) / LLM 网关 / 状态机 / orchestrator / MCP 三道闸+结果闸 / os_ops 13 工具 / api 层 | L | 🟢 真实 |
| PolicyEngine.evaluate | D | 🟢 **PR1 合入 + 已接线切真**：app(build_gateway)注入 PolicyEngine(DEFAULT_POLICY, registry)+真 os_ops 工具集；L1 哨兵/F005 真引擎下全绿 |
| Executor.execute | D | 🟢 **已接线切真（接线里程碑·任务乙）**：build_gateway 注入真 PrivilegeExecutor；app 跑真命令（方案B）；config.diff 暂从注册集摘除 |
| AuditSink.append 落库 | D | 🟢 **已接线切真（接线里程碑·任务甲）**：get_audit 升 lifespan 单例 SqliteAuditSink（持 DB 句柄+链状态，shutdown close）；端到端 verify_chain 真过+真篡改检出 |
| RBAC 审批授权 | D/L | 🟢 **授权 + 审批认证均真（50dbfcf）**：can_approve fail-closed + 审批认证出自反代签名身份(auth.py HMAC)，伪造角色无效；**审批闸 BLOCKER 已闭合**。全量端点认证(verify_token)仍内网态，待下一安全增量。 |
| RCAEngine | X/L | 🟢 **B2 已接线切真**：app.get_rca()→DefaultRCAEngine，chat 注入 + 独立端点接真（f4b78d1）|
| 前端 | X | 🟡 前端骨架/rejected 适配进行中 |

---

## 1. 冻结接口与签名（不得擅自漂移）

- `Orchestrator(*, llm, gateway, audit, events, rca=None, trace_id=None, max_observation_rounds=3)`；
  `async run(messages, user_intent="")`；`async resume(approved)`。
- `MCPGateway(registry, policy, executor)`；`list_tools()`；`is_read_only(tool)`；
  `evaluate(tool)->PolicyVerdict`；`async call(tool,*,approved=False)->CallOutcome`。
- `PolicyEngine.evaluate(tool: CandidateTool) -> PolicyVerdict`（**同步**，contracts/policy.py 冻结）。
- `Executor.execute(tool)->ToolResult`（async；方案 B：普通失败用 exit_code，系统级故障才 raise）。
- `AuditSink.append(record: AuditRecord)`（只落库，**不重算哈希**；curr_hash 由 orchestrator 构造好）。
- `RCAEngine.analyze(evidence: list[ToolResult]) -> dict`（空 dict→不 emit rca 事件）。
- 哈希链：`curr=SHA256(prev ∥ canonical_json(payload))`，复用 `contracts.audit.compute_curr_hash`/`canonical_json`，**绝不另写序列化**。

---

## 2. 跨窗口关键决策（已拍板）

1. **判定语义 == 执行语义（最高架构铁律）**：路径/参数归一抽成 `backend/app/security/normalize.py`
   公共纯函数；evaluate 与 Executor **import 同一份**，杜绝判执不一致/TOCTOU。
   - evaluate 只做**纯词法** `posixpath.normpath`（无 IO）；真实 realpath/symlink 兜底在 Executor + systemd 沙箱。
   - 路径大小写**敏感**（Linux 现实）；**禁**全角→ASCII 等自创归一。
2. **approval_role 全程小写** `operator/admin`，与 `ToolSpec.requires_roles` 一致。契约无 `required_role` 字段；
   前端要大写是展示层的事，策略输出不迁就。
3. **protected_paths 取值**：递归扫描所有"形如 `^/` 绝对路径"的标量参数值，**不是**只认硬编码字段名
   （防未来工具用别的参数名静默绕过）。
4. **final_risk** = `max(spec.risk, 命中规则隐含风险)`，deny 类至少 R3/R4，给死映射保证可复算。
5. **/etc 前缀保护**(forbid_modify)只对**变更类**工具生效；读 /etc/passwd|shadow 靠 FILE001(大小写敏感)。
   `/etc/PASSWD` 对只读工具不命中（Linux 下也非同一文件）；测试区分变更 vs 只读。
6. **ARGS_TOO_LARGE**：放 guard 内置兜底（非 DEFAULT_POLICY 规则），返回稳定 `matched_rules=["ARGS_TOO_LARGE"]`，阈值给宽。
7. **D 三 PR 串行**：PR1 evaluate → PR2 Executor(2a 本地/2b 真沙箱) → PR3 Audit 落库。Audit 最可延后。
8. **PolicyEngine 构造签名**：`RuleBasedPolicyEngine(policy: PolicySet, registry: ToolRegistry)` + 导出 `PolicyEngine` 别名。
9. **★RBAC 真实认证源 = BLOCKER-for-deployment（最高优先级，原"待定"提级）**：接线里程碑·任务丁把 `can_approve`
   接进 resume 做**授权**确定性强制（fail-closed），但调用者角色来自演示态 `X-User-Role` 裸头、**可伪造、无身份验证**。
   叠加真执行器 = 被伪造角色驱动真变更的风险。**拍板**：① 联调期维持"内网/联调 only"锁定，**不对外暴露**；
   ② 部署前**必须**接"经验证身份派生角色"。**✅ L 已拍板选型 = (a) 反向代理/网关注入已验证身份**（麒麟统一认证/LDAP），
   app **只信任来自可信代理的签名头**（拒绝直连绕过代理的请求；签名/共享密钥校验代理身份），由签名头派生角色再喂 `can_approve`。
   （备选 (b) JWT / (c) mTLS 暂不采用。）**角色必须来自已验证身份，不得来自裸客户端头**；演示态 `X-User-Role` 仅联调用、上线前替换。
   "认证未接入"全局告警在真认证接入前保留。落点：§5 开放项标 BLOCKER-for-deployment（选型已定，待接线增量落地）。
10. **overview 只读采集豁免哈希链审计（方案 b，有界）✅ 签字采纳**：概览探针经 gateway 直接执行只读工具、不进哈希链。
    理由：概览非一次 agent 请求/trace（无 intent/无审批/无状态变更）、高频轮询，塞进 per-trace 哈希链语义不符且膨胀。
    豁免被"仅 `is_read_only`"**硬护栏**锁死（已测：变更工具即便 allow-all 策略也被拦，不出现在 probed_tools）+ TTL(5s) 节流 + data_source 标注。
    与"每次**变更**留痕"核心叙事不冲突。GAP-1 = 已定（方案 b）。
11. **审计库路径**：dev 默认相对 `./data/audit.db`（依赖 CWD，dev 可接受；`*.db` 已 gitignore）。
    部署：改**可配置**（env 如 `KYLIN_AUDIT_DB`）+ **绝对路径** + **受限文件权限**（审计库是防篡改证据，完整性要紧）+ 留 retention/rotation 待办。
12. **D-10 输入闸接线口径（✅ 已拍板）**：在 orchestrator 首次 `plan()` **之前**调 `detect_injection(user_input)`。
    - `severity=="high"` → **deny**（终态 REJECTED，emit `rejected`，cause 区分注入；审计 payload 写 `category`+`pattern_id`+原文引用）。
    - `severity=="medium"` → **审计标记后放行**（写审计风险标注，**不**走 confirm——"谁来审批一个疑似注入"语义不成立、且 WAIT_APPROVAL 需 approval_role）。**不采用** D 原议的 medium→confirm。
    - **★红线定位**：本检测器是**防御纵深，不是主防线**。安全地板永远是确定性五道闸；接线**只能增加限制，绝不授予信任**——"未检出注入"≠"输入可信"，检测通过**不得短路任何下游闸**。
    - 审计写入用 user_input **原文**；`matched_span` 是 NFKC 归一后子串，仅作命中证据（O9）。
13. **拒批权限放宽（✅ 已拍板）**：`resume(approved=False)`（拒批=取消，安全方向）**对任何已认证调用者放行**，不校验 approval_role；**仅 `approved=True`（批准）才过 `can_approve`**。低权角色可取消高权待批计划。
14. **审计 REST 字段命名（✅ 已拍板）**：**端到端保契约原名**（`phase`/`payload`/`valid`...），**不做翻译层**（少一层漂移）。不采用 phase→record_type/payload→content 改名。DB 层与 API 层均沿用契约原名，D 无需再动。
    测试卫生：已加 `backend/tests/conftest.py` autouse 夹具把 `_AUDIT_DB_PATH` 钉到 `:memory:`，杜绝 audit.db 落工作树。
15. **ADR-0005 demo-record-mode（✅ 已拍板）**：`get_llm()` 加 `KYLIN_LLM_RECORD` env
    分支——`true` 时返 `LLMAdapter(completion_fn=RealLLMClient().completion_fn)`（录制
    模式），默认（未设 / `false`）仍走 `build_fake_llm()`（ADR-0003 demo-only
    锁死）。**生产 `KYLIN_LLM_RECORD` 永远 false**（仅 demo 录屏 / 内网联调 opt-in）。
    `/api/llm/health?probe=true` 不受影响。守门语义：D2 §5 红线原 3 用例保留
    （T9/T10/completion_fn_spy 默认 fixture 强钉仍守），新增 T11
    `KYLIN_LLM_RECORD=true` → spy >= 1（录制模式显式放行）。详见
    `docs/adr/0005-demo-record-mode.md`。**不破 ADR-0003**——revisit 条件：
    项目正式完成 = 阶段5 step 2 全套 6 commit 收口后撤 ADR-0005 + ADR-0003。

---

## 3. 集成切换点（fake→real，L 负责接线）

全部收敛在 `backend/app/api/_fakes.py` + `app.py` 的 provider：
- **策略**：`build_gateway()` 注入 `PolicyEngine(DEFAULT_POLICY, registry)`（同一 registry）。✅ 已切真。
- **执行**：`build_gateway()` 注入真 `PrivilegeExecutor()`。✅ **已切真（接线里程碑·任务乙）**——
  函数 `build_fake_gateway` 已诚实重命名为 `build_gateway`；config.diff 经 `_DEFERRED_TOOLS` 从注册集摘除（无单命令模板，避免"注册却跑不了"）。
- **审计**：`app.get_audit()` ✅ **已升 lifespan 单例 SqliteAuditSink（接线里程碑·任务甲）**——持 DB 句柄+链状态，shutdown 调 close；
  接线签名：`SqliteAuditSink(db=路径或 sqlite3.Connection)` / `append(record)` / `verify_chain(trace_id)->ChainVerifyResult(.valid/.broken_seq/.reason)` / `last_hash(trace_id)->str(空链返 GENESIS)` / `close()`。
- **RBAC**：`api/deps.verify_approval_role` ✅ **已接 can_approve（接线里程碑·任务丁）**——取 `X-User-Role` 归一小写、resume 前 fail-closed 强制。
  ⚠️ 仅**授权**真，**认证源未接、演示态头可伪造**（§2.9 BLOCKER-for-deployment）。
- **RCA**：`api/routers/rca.py` ✅ **已切真（B2, f4b78d1）**：`DefaultRCAEngine`；chat 经 `Depends(get_rca)` 注入；独立端点 `analyze_problem`。模块级 `_reports` 仍内存版。
- **overview 审计口径**：✅ **已定（方案 b，§2.10）**——只读探针显式豁免哈希链 + 硬只读护栏 + TTL + data_source 标注。

### L1 集成验收哨兵
`backend/tests/test_e2e_real_policy.py`：**已激活并 5/5 通过**（D 的 PolicyEngine 合入后）。
当 D 的 PolicyEngine 从 `backend.app.security` 导出即自动激活。已按 `(policy, registry)` 预对齐。
断言即规格：R0/R1 allow→VERIFIED、R2→confirm/operator、R3→confirm/admin、deny 整批 REJECTED、
原子计划含 deny 不部分执行——全部对 D 真 evaluate 验证通过。

### ✅ L 接线已完成（PR1 已切真，app 走真策略闸）
`build_fake_gateway()` 已注入 `PolicyEngine(DEFAULT_POLICY, registry)` + 真 os_ops 工具集；
运行中的 app（/api/chat、/api/tools/call）真正经策略闸裁决（test_tools_call_real_policy_deny_via_api
实证 /etc/shadow 经 API 被真策略 deny）。**唯一剩余切换点**：`executor=FakeExecutor()` → D 的真 Executor(PR2)。
get_audit() 待 D 的 AuditSink(PR3) 合入时升为 lifespan 单例。

---

## 4. CI 铁律（反复爆红的根因，务必守）

- 验证环境用 **`kylin_ci`**（CI 镜像 = Python 3.11 + `requirements.txt` + `requirements-dev.txt`），**严禁用 `kylin` 杂 env**。
- 验证命令：`$env:PYTHONUTF8="1"; conda run -n kylin_ci --no-capture-output pre-commit run --all-files`
- **验证前先 `git add`（或 `git add -N .`）**让未跟踪新文件进入 pre-commit 视野——否则 `--all-files` 跳过未跟踪文件 = 本地假绿、CI 红。
  （已建手动 hook「CI 镜像预检（含未跟踪文件）」自动做 `git add -N` + 跑四道闸。）
- push 前确认：四道闸全 Passed + 无 "files were modified" + `git status` 干净。
- 提交纪律：执行窗口/协作者**从不自行 commit**；写改动清单+自检+diff 进 `审阅交接.md`，审阅 PASS 后由 L 提交。

---

## 5. 各窗口进行中 / 开放项

- **D**：✅ PR1 evaluate 已合入 dev(bb1721d)。下一步 **PR2 Executor**（首稿已有，import security/normalize.py 同源）。
  PR2b 真沙箱依赖麒麟 VM 访问权限（待确认）。injection_detector / 红队 benchmark 单独排期。
- **X**：前端接真 API（按 stream.py 事件契约）、RCA playbook(mcp_servers/rca)、deploy/、demo/。
  后端 fake 现可端到端跑：`conda run -n kylin_ci uvicorn backend.app.api.app:create_app --factory --port 8000`。
- **L**：✅ 内核多轮 observe→re-plan、L1 哨兵(已验证)、演示剧本、**D16 四场景骨架 + D21 设计文档
  (docs/design/) + D19 功能矩阵脚手架** 全部入库(a9c0a38)。F005 已激活验证真策略 deny。
  **下一步：接线切真 PolicyEngine**(见 §3 待办)。
  待办：定协作模式；D 的 Audit 合入后接线升 get_audit 为单例。
  待决策(记录)：场景③ 是否补 I/O 感知工具(io.stat/io.top)——建议作 backlog，Executor 真件到位前不急；
  F001–F006 编号——采纳当前 functional-test-matrix.md 提案为工作基线。

### PR3 收口项（已落定）
- 审计查询 API 字段命名 → **✅ 已拍板（决策 14）：端到端保契约原名（phase/payload/valid），不做翻译层**。DB 层与 API 层均沿用，D 无需再动。
- **RCA 端点审计口径（B2 审阅引出，与 GAP-1 同类）**：`POST /api/rca/analyze` 接真 DefaultRCAEngine 后，
  从"永远空报告"变为"产真实报告且不产哈希链审计"——又一条绕审计的路径（只读不执行、报告是建议性）。
  **倾向比照决策 10（overview 方案 b）显式豁免只读建议路径 + 写明理由**；主链路 orchestrator VERIFIED 后 emit "rca" 事件亦无 audit_appended（D15 设计，报告基于已审计 evidence 派生）。
  均非 S/C 类违规，登记备查，接审计相关增量时一并标注豁免理由。

### 夜间非阻塞推进（已审 PASS，待 commit&push）
工单 `夜间长任务-执行窗口-L侧非阻塞推进.md` 四任务 A/B/C/D + 审阅引出的 6 处 gap 已全部落地并经审阅窗口独立核验（四道闸全绿，pytest 252 passed/1 skipped=Executor 哨兵精确待命）：
A. Executor 维度端到端真验证哨兵（精确待命，D 的 PR2 合入即激活，含 symlink/TOCTOU 占位）；
B. 功能矩阵 F001–F004 接真 PolicyEngine（脱离 RiskBasedPolicy 桩）+ F006 改真 SSE 行为测试；
C. mypy 扩面到 agent/mcp/llm/api **+ mcp_servers/os_ops**（不含 D 的 security）+ CI pip/pre-commit 缓存；
D. overview 接 gateway 真实采集管道 + TTL 节流 + per-probe 兜底 + data_source/probed_tools 桩态标注。

#### ★Executor 切真前必决项（勿遗忘；FakeExecutor→PrivilegeExecutor 接线前必须全部清掉）
- **【PR3 已合入·接线协调项 a — resume/审计续写非幂等】**：D 的 `SqliteAuditSink.append` 对重复 `(trace_id, seq)`
  抛 `sqlite3.IntegrityError`（UNIQUE 约束，非幂等）。L 的 resume/重入（L-3）接线**必须**用
  `last_hash(trace_id)` 取已落库最后链值、从下一个 seq 续写，**不能重放已落 seq**（否则崩）。
  `last_hash` 无记录返 GENESIS_HASH，正可作 resume 起点。**接线时 orchestrator 当前 `_seq`/`_prev_hash`
  内存自增需与落库实际续上**——单例化 AuditSink 后，resume 续跑前应从 `last_hash` 重建 prev、从 max(seq)+1 起。审阅窗口待接线时复核。
- **【PR3 已合入·接线协调项 b — 审计 REST 列映射】**：表含 `id`(自增) / `created_at`(ISO8601) / `phase` / `payload`(canonical_json 文本) /
  `prev_hash` / `curr_hash`。L 的 Trace 明细查询按 CLAUDE.md §9 映射 `phase→record_type` / `payload→content` / `id→id` / `created_at→created_at`；
  verify 接口返 `valid`（非 ok）。**字段命名端到端是否保契约原名 vs 翻译，仍由 L+X 签字**（见下「待 PR3 收口」原项，倾向保原名）。
- **【✅ 已闭合·PR2整改版 54b5667】symlink 越权 / 判执不一致**：D 加 `path_policy.real_path_violation()`——对 realpath
  真实目标重跑 `classify_paths`(保护路径) + arg 级 deny 规则(FILE001)；`_sanitize_arg` 在 `real != 词法路径` 时调用，
  命中即 `_PathTraversalError`→方案B 拒绝。Executor 经 `policy=DEFAULT_POLICY` 构造拿配置、不依赖 api 层。
  **真 symlink 测试**（`test_real_executor_symlink_escape_guarded`：真 `symlink_to(/etc/shadow)`、不 mock realpath、
  断言子进程未起 + FILE001）已证明。审阅窗口逐行核验通过。残留 O5（见下）。
- **GAP-1 概览执行的审计口径（★决策：与 Executor 接线同批，在 PR3 之后做）**：`api/routers/system.py` 的 overview 探针经
  `gateway.call` 直接执行只读工具、**不经 orchestrator → 不产哈希链审计**。Executor 切真后这会成"绕过审计的真实执行路径"。
  **L 拍板：Executor 接线（FakeExecutor→PrivilegeExecutor）放在 D 的 PR3 AuditSink 落库之后**——届时真执行与真审计一起到位，
  避免"真执行却无真审计"的中间态；overview 审计口径同批解决（倾向 (a) 概览采集也走审计留痕；或 (b) 显式豁免+理由）。
  现已先落 TTL(5s) 节流防命令风暴。
- **GAP-3 `backend.app.executor` 冻结导出名 ✅已应验**：D 实际导出 `PrivilegeExecutor`（无 'd'），与哨兵猜测的
  `PrivilegedExecutor` 不符 → 合并后哨兵本会静默不激活（正是预警的 L-2 反模式变体）。审阅窗口已在哨兵 `_EXECUTOR_SYMBOLS`
  补 `PrivilegeExecutor` 使其激活（合并后 3 真断言 pass）。**仍建议 L+D 在 §1 冻结导出名**，PR2b 接线清单含"确认哨兵激活非 skip"。
- **config.diff 无命令模板**：D 的 COMMAND_TEMPLATES 覆盖 12 工具，缺 config.diff（需基线对比，非单命令）→ 真 Executor 调它返回
  127 "not-in-whitelist"。切真前需特殊处理（如 system.info 式聚合）或暂从 registry 摘除，避免注册了却跑不了。
- **SAFE_ENV / 命令绝对路径需麒麟 V11 实机校准**：command_templates 硬编 `/usr/bin/...`、SAFE_ENV PATH 为通用 Linux 路径；
  LoongArch/麒麟实机 `which` 输出可能不同（如 /usr/local/kylin/bin），D 已注明拿到 VM 后精确，接线前核对。
- **GAP-6 通知 X（跨窗口）**：`SystemOverview` 新增 `data_source`/`probed_tools` 字段（向后兼容，带默认值），
  X 前端需加"桩态"徽标渲染。

#### PR2 审阅引出的 O5/O6 处置（已回复 D，均非 PR3 阻断）
- **O5 realpath 复核 spec 退化**（防御纵深 minor）：`real_path_violation` 内 `classify_paths(spec=None)`→名字启发式判变更类。
  影响面：deny 路径（FILE001 shadow/passwd 等 arg 级 deny）对**所有工具**仍拦（关键场景安全）；当前变更类工具在 `_CHANGE_TOOLS`
  启发式集内已覆盖；残留仅"未来新增、不在启发式集、symlink 指向非 deny 保护目录的变更类工具"。**决策**：真兜底=PR2b systemd 沙箱；
  不动 Executor 冻结签名去拿 spec。→ 见下「新增工具检查单」。
- **O6 哨兵归属**：`test_e2e_real_executor.py`(L 编排层哨兵) 与 `test_executor.py`(D 执行层单元) 层次不同、不重复。
  D 的真 symlink/TOCTOU 用例保留；**L 侧待办**：把 L 哨兵里两个 skip 占位改造为编排层真断言（整链跑、断言 orchestrator
  因 executor 拒绝而出 error/不执行），与 D 的执行层用例分层互补、消除同名分叉。该改造放 Executor 接线里程碑时一起做。

#### ★新增工具检查单（O5 决策派生，新增任何工具必走）
1. 写 `ToolSpec`（risk/requires_roles/reversible）；变更类（risk≥R2 或 reversible=False）**必须**同步加进
   `path_policy._CHANGE_TOOLS`（或确保工具名含 delete/remove/clean 等 hint）——否则 Executor realpath 复核会漏判变更类。
2. 在 `executor/command_templates.py` 加命令模板（除 config.diff 类需聚合的特殊工具）。
3. 跑 `test_executor.py::test_all_os_ops_tools_have_template`（缺模板会红）。

醒后由 L 提交（审阅窗口已分 4 要点 commit：fa92df0/2f7a131/9696649/d9cfcd4；**push 因 github 443 网络抖动暂未成功，待网络恢复重推**）。

---

## 6. 全量架构审计结论 + L 侧整改登记（dev=db3be03，审阅窗口独立复核）

> 来源：`架构审计报告-dev-db3be03.md` + 三份审阅指导（L/D/X）。审阅窗口对 L 侧 7 条**逐条独立核验**
> （grep/read/跑命令），**全部坐实**，非臆测。D/X 侧由其各自窗口照报告整改。
> 总体定性：**技术路线正确、策略闸是真材实料（非安全剧场）；但安全闭环只完成"判定半边"，执行+审计落库全桩。**
> 对外口径锁定：**"安全裁决已闭环、安全执行落地中"**——禁止宣称已是产品级安全闭环。

### 6.1 测试数字祛魅（口径统一）
- 真相：**自研测试 246 collected / 246 passed / 0 skipped**（kylin_ci, Py3.11；23 文件）。
- `third_party/linux-mcp-server/tests` 缺 `fastmcp` 跑不起来，**不贡献 passed**；历史"411/57/72…"等数字
  含上游或为历史快照，对外一律以"项目自研测试=246"为准。`审阅交接.md` 内逐增量历史数字保留作留痕，不再引用为现状。
- ~25–30 条 "executed=True/exit_code==0/verified/FINISHED" 成功语义**全部来自 FakeExecutor 罐头**——
  套件当前证明的是"不该执行的确实没执行"（真），"执行后是否安全"零真实覆盖（待 PR2）。

### 6.2 L 侧整改登记（owner=L，执行窗口落地；审阅窗口已核验属实）
| # | 严重度 | 问题（已核验） | 状态 |
|---|---|---|---|
| L-1 | 🔴 Blocker | `mcp==1.27.2` 传递 jsonschema→rpds-py(Rust)、pyjwt[crypto]→cryptography→cffi(C)；req:18 注释失真；mcp 全仓仅用 `mcp.types.Tool` 一处 | ✅ 已修（自写 @dataclass Tool，backend+mcp_servers 零 mcp 导入，审阅核验）待提交 |
| L-2 | 🟠 High | L1 哨兵 `except ImportError` 过宽 → 环境损坏伪装 skipped 下线 | ✅ 已修（删两处守卫，实跑 passed 非 skip）待提交 |
| L-3 | 🟠 High | 三处 docstring 虚标"已测"：409 双审批守卫 / SSE 心跳 / runner 异常兜底（grep 零引用证实） | ✅ 已修（补 5 个真用例，grep 现有命中）待提交 |
| L-4 | 🟡 Med | `resume` 并发重入竞态（状态检查与异步翻转非原子）→ 破坏审计/SSE 完整性 | ✅ 已修（begin_resume CAS+409+finally release，并发用例）待提交 |
| L-5 | 🟡 Med | CI：pytest 容忍 exit5 假绿 / mypy hook pydantic 未 pin / `.gitlab-ci.yml` 双轨 | ✅ 已修（pytest==0/mypy pin 2.13.4/gitlab DEPRECATED）待提交 |
| L-6 | 🟡 Med | REJECTED 终态不发终态事件 → 真实前端永久转圈 | ✅ **方案B 已实现**（stream.py 加 rejected EventType + orchestrator 两路 emit；本地 6510f7b/6f0df2c 待 push）|
| L-7 | 🔵 Low | audit payload 契约"固定字段"声明与自由 dict 实现不符 | ✅ 已修（docstring 改"随 phase 变化"，零 schema 改动）待提交 |
| L-8 | 🔵 Low | requirements 仅 pin 5 顶层包，传递依赖全浮动 | ✅ 已修（干净 venv freeze 出 constraints.txt，CI/部署接 -c；确认无 mcp/rpds/cryptography）已 push |
| L-9 | ⚪ Nit | file.lsof_check 互斥虚假承诺 / overview·rca 桩 / os_ops 措辞过宽 | ✅ 已修（lsof/os_ops 措辞/演示页脚）待提交 |

### 6.3 跨窗口实质漏洞（非纯 L，需协同）
- **两端 RBAC 同时失效**（owner=D+X）：后端 `verify_approval_role` 占位放行 + 前端 `required_role` 漂移(违反决策§2.2)
  + 前端默认角色 Admin → 人工确认闸访问控制形同虚设。D 接真校验 + X 回归 `approval_role`(小写)。
- **前端从未与真后端联调**（owner=X）：3 个 Blocker（/api/api 双前缀、resume 带 comment 撞 extra=forbid、SSE done 未监听）
  皆"开后端跑一轮即暴露"。强制里程碑：关 mock 联调一轮。
- **L-6 REJECTED 终态契约**：L 补终态事件 + X done 统一清 loading，需 L+X 当面对齐。

---

最后更新：**接线里程碑（甲审计单例 / 乙 Executor 切真 / 丙 overview 方案 b / 丁 RBAC 授权）已在工作区落地，经审阅窗口核验、待 commit**（基线 dev=`264f278`，四道闸全绿、312 passed/7 skipped）。
- **安全闭环"后半条"实质闭合**：真执行(乙)+真审计落库(甲，lifespan 单例 verify_chain 真过+真篡改检出)同批到位（决策2 不留"真执行无真审计"中间态）；审批授权(丁)由 can_approve fail-closed 强制；overview 只读豁免(丙，方案 b 已签字)。
- **★对外口径更新**：审计闸⑤ 落库已真、人工确认闸③ **授权已真**——但 ③ **认证源未接（演示态 X-User-Role 头可伪造）= BLOCKER-for-deployment**（§2.9），联调期"内网 only"，部署前必须接经验证身份派生角色。
- 本批 advisory 补强已落：`conftest.py` autouse 把审计库钉 `:memory:`（杜绝 audit.db 落工作树）；`_most_restrictive_role` fail-closed 加固（任一 confirm role 未知/None→整批 None→无人可批）；`_fakes` docstring 笔误（build_fake_gateway→build_gateway）修复。
- 戊（dispatch 解析接 overview/观测，数值转真 data_source→real）= L backlog（非阻断）；config.diff 聚合 = backlog（已知会 X：前端工具列表少一项）。
- 拒批权限放宽（approved=False 是否豁免 can_approve）= **L 待定**（当前拒批=取消亦需审批权限，方向安全）。
下一审：接线里程碑这批的正式提交 + 部署前 RBAC 认证源（BLOCKER）。

---

最后更新（历史）：dev=`264f278`（**已 push**；D 的 PR3 AuditSink 经审阅窗口独立核验 PASS 已 merge：SqliteAuditSink 落库 + verify_chain 链校验真，S7/F3/E3/E4 全过；四道闸全绿、294 passed/7 skipped）。
- **审计闸⑤ 算真→落库真**：哈希链计算 + SQLite 落库 + 链校验（防篡改/重排/断链/缺号）全到位；唯余「get_audit 升 lifespan 单例」接线归 L。
- **Executor 接线里程碑现已解阻**：D 的 PR2(执行)+PR3(审计) 均入库 → 可一次性做 FakeExecutor→PrivilegeExecutor 切真 + get_audit 升单例 + can_approve 接 deps.py + GAP-1 overview 审计口径 + config.diff 处置（见「Executor 接线里程碑」清单，别拆散）。
- 残留 advisory：RCA 端点/主链路 rca 事件审计口径（B2 引出）；接线协调项 a/b（见上「★Executor 切真前必决项」）。
下一审：L 的 Executor+审计+RBAC 接线里程碑（届时盯 resume 用 last_hash 续写、overview 走审计、单例 close 收口）。

---

最后更新（历史）：dev=`f4b78d1`（**已 push**；执行窗口 甲B2-RCA接线 + 乙O6哨兵 经审阅窗口独立核验 PASS、四道闸全绿、278 passed/7 skipped 已合入）。
- **甲 B2**：RCA 切真（get_rca provider + chat 注入 + 独立端点 analyze_problem）。RCA 红线核验：仅 emit 报告不执行。
- **乙 O6**：Executor 哨兵两个 symlink/TOCTOU 占位升级为编排层真断言（win32 跳过；逻辑经审阅逐链追到 path_policy/policy_loader 复核，
  `confirm_required=["/home"]`→PATH_CONFIRM_REQUIRED、FILE001→/etc/shadow 坐实，待 CI ubuntu 实跑确认）。O6 同名分叉消除。
- 残留 advisory（非阻断）：RCA 端点/主链路 rca 事件审计口径（见上「待 PR3 收口」）。
**Executor 仍未接线**（app 用 FakeExecutor）——接线里程碑仍待 D 的 PR3 AuditSink 之后一次性做。
下一审：D 的 PR3 AuditSink（S7 append 不重算 hash/复用 canonical_json + F3 verify 返结构化 valid + 真篡改用例）。

---

最后更新（历史）：dev=43a31ab（**已 push**；D 的 PR2 整改版已 merge：symlink 判执一致闭合 + RBAC 纯函数 + 策略层 D-4~D-8；哨兵激活 3 真断言；四道闸全绿）。
**Executor 仍未接线**（app 用 FakeExecutor）——接线是下一里程碑，需同步：D-3 deps.py 接 can_approve（L）、GAP-1 overview 审计口径、config.diff 处置；symlink 已不再是接线阻断。
残留 O5：realpath 复核用 spec=None→名字启发式判变更类，未来新增变更类工具+symlink 到非 deny 保护目录可能欠拦（deny 路径如 shadow/passwd 对所有工具仍拦）；真兜底 PR2b 沙箱。
残留 O6：哨兵 symlink/TOCTOU 占位仍 skip，D 的 test_executor.py 有等价真用例（覆盖等价）。
下一审：D 的 PR3 AuditSink（盯 append 不重算 hash/复用 canonical_json + verify 返回 valid + 真篡改用例）。


---

## ★最新权威状态（dev=`0d4347b`，审阅窗口收口，2026-06-13）

> 本块为当前唯一权威"最后更新"；上方各 "最后更新（历史）" 块仅留痕。

**已 merge 入 dev 的链（origin 同步）**：
- `5267560` 接线里程碑（甲审计单例 / 乙 Executor 切真 / 丙 overview 方案b / 丁 RBAC fail-closed）。
- `ee43dc3` D-10 输入闸 injection_detector + 红队 golden（**dormant 未接线**）。
- `0d4347b` feat/x：rejected 前端适配(方案B) + lockfile 公网重建 + overview 桩态徽标 + RCA 测试加固；CI frontend-build 转硬闸。

**五道闸**：①输入🟢(+D-10 检测器待接) ②策略🟢 ③确认🟡(授权 fail-closed 真/**认证源未接=BLOCKER**) ④结果🟢 ⑤审计🟢(落库单例真)。

**★部署前唯一硬阻断（决策⑨ BLOCKER-for-deployment）**：RBAC 真实认证源未接——切真执行器 + `X-User-Role` 头可伪造 = 可被伪造角色驱动真变更。联调内网 only。**✅ 选型已定 = (a) 反向代理注入已验证身份 + app 只信可信代理签名头**（麒麟统一认证/LDAP）；角色须出自已验证身份。待接线增量落地。

**已拍板（写定）**：⑨ 认证源=反代签名头(a)；⑩ overview 只读探针豁免哈希链(方案b)；⑪ 审计库 dev=`./data/audit.db`、部署=env+绝对+受限权限+retention；⑫ D-10 接线 高→deny/中→审计标记放行(不走confirm)、防御纵深只增限制；⑬ 拒批放宽(approved=False 任何已认证者可取消，仅批准校验角色)；⑭ 审计字段端到端保契约原名(不翻译)；config.diff 暂摘注册集(已知会 X)。

**backlog（非阻断）**：① **接线增量(L)**：RBAC 真认证(反代签名头→角色→can_approve) + D-10 输入闸接 orchestrator 首 plan 前(决策⑫口径)；② 戊 dispatch 解析(overview 数值转 real)；③ config.diff 由 L 在 mcp 层聚合(决策⑤)；④ PR2b systemd 沙箱(待麒麟 VM，D)；⑤ frontend vitest 补跑(X，chat-security.test.ts 本机未跑)。

**验证基线**：dev=`812c7c6`（含 CI frontend-build 修复：`npx --prefix` 坏调用→working-directory）；kylin_ci 四道闸全绿、pytest 342 passed/7 skipped；前端 npm ci+vue-tsc+vite build 全 exit=0（本机复刻确切 CI 命令验证）。

**验证基线**：kylin_ci 四道闸全绿、pytest 342 passed/7 skipped；前端 npm ci+vue-tsc+vite build 全 exit=0（审阅窗口本机真跑）。

**协作纪律提醒**：D/X 两次出现"报告全绿但本地未验证"（X feat/x 实有 ruff 2×E501+格式，由审阅窗口集成修复）；协作者无法本地跑闸时应如实写"未验证"，不得报"全绿"。


---

## ★最新权威状态（dev=`50dbfcf`，审阅窗口收口，2026-06-13 之二）

> 取代上方所有"最新/历史"块为当前权威。

**链（origin 同步）**：…→ `50dbfcf` 认证+输入闸接线增量（甲 RBAC 反代签名认证 / 乙 拒批放宽 / 丙 D-10 输入闸 / 丁 fake planner confirm / 戊 plan_generated tool）。

**五道闸**：①输入🟢（+D-10 已接：high→deny/medium→flag，拦在 LLM 前） ②策略🟢 ③确认🟢（授权+审批认证均真，**审批闸 BLOCKER 闭合**） ④结果🟢 ⑤审计🟢。

**★审批闸 BLOCKER 已闭合**：审批授权出自反代签名身份（auth.py HMAC，fail-closed，防重放，常量时间比较），伪造 X-Auth-Roles 无密钥即 401。`KYLIN_AUTH_MODE` 默认 `proxy`（生产，无密钥 fail-closed）/ `dev`（联调，裸 X-User-Role + 告警）。

**★下一安全项（部署前最后一关）= verify_token 全量端点认证**：当前仅审批闸接了签名认证；`/api/chat`、`/api/system/overview`、`/api/tools/*`、会话 CRUD 等**仍 verify_token 占位（anonymous 放行）**。即只读真执行仍零认证 → **app 部署前仍须内网 only**，直到 verify_token 也接签名身份。已起工单 `夜间长任务-执行窗口-verify_token全量认证.md`。

**部署待办（汇总）**：`KYLIN_PROXY_AUTH_SECRET` + `KYLIN_AUTH_MODE=proxy` + 反代按 `auth.sign_identity` 口径给**所有**入站请求注入签名身份头（不止 /approvals）；`KYLIN_AUTH_DB`(审计库) 绝对路径+受限权限；dev 模式严禁上生产。

**backlog（非阻断）**：戊已并入 50dbfcf（plan_generated tool）→ 实际 backlog 仅余：① verify_token 全量认证（下一增量，已起工单）；② overview 真实数值 dispatch 解析（data_source→real）；③ config.diff mcp 层聚合；④ PR2b systemd 沙箱（D，待麒麟 VM）；⑤ frontend vitest 补跑（X）。

**协作纪律提醒（再记）**：协作者报告数字以审阅窗口亲跑为准（本批执行窗口报 342、实际 361）；无法本地跑勿报"全绿"。

**验证基线**：kylin_ci 四道闸全绿、pytest 361 passed/7 skipped；前端 npm ci+vue-tsc+vite build 全 exit=0（审阅窗口本机真跑）。


---

## ★最新权威状态（dev=`6b5fecb`，审阅窗口收口，2026-06-13 之三）

> 取代上方所有"最新/历史"块为当前权威。

**链（origin 同步）**：…→ `50dbfcf`（认证+输入闸增量）→ `6b5fecb` **verify_token 全量端点认证 mode-aware**（proxy 反代签名 fail-closed / dev 联调放行）。

**五道闸 + 认证**：①输入🟢（D-10 已接 high→deny/medium→flag） ②策略🟢 ③确认🟢（授权+审批认证均真，审批闸 BLOCKER 闭合） ④结果🟢 ⑤审计🟢（落库单例真）。🔐 **全量端点认证🟢（6b5fecb）**。

**★verify_token 全量端点认证已闭合（部署前最后一道安全关）**：`deps.verify_token` 由"anonymous 占位放行"改为 mode-aware——
- `proxy`（默认，安全兜底）：所有挂 verify_token 的端点（/api/chat、/api/system/overview、/api/tools/*、会话 CRUD、**SSE /api/chat/{trace_id}/events**）要求反代签名身份，缺/非法 → **401 fail-closed**（复用 `auth.verify_proxy_identity`，未新写验签）。
- `dev`（联调）：放行返 `"dev"`，告警降 `logger.debug`（不每请求刷屏）；conftest 默认 dev，既有端点测试无需加头。
- **verify_token 只认证**（确认存在已验证身份即返 `p.user`/str），不取角色、不加授权门槛；角色授权仍归审批闸 `require_proxy_identity`+`can_approve`，二者并存。
- app.py lifespan/description 告警措辞改诚实（不再写"认证未接入"）。

**审阅窗口独立核验（亲跑，非收报告）**：C3 仅动 `api/{app,deps}.py`+`tests/`，未碰 contracts/auth.py/{security,executor,audit,db}/frontend/mcp_servers；四道闸全绿、无 "files were modified"；**pytest 真值基线 342 → 351 passed / 7 skipped（+9 新认证用例，零回归）**。
- ⚠️ **数字纠偏**：执行窗口报告"370（基线 361+9）"两个绝对数均虚高约 19；备忘旧记"361"亦为虚高/旧数。**真实基线 = 342**，本增量后 = **351**。今后一律以审阅窗口亲跑 342→351 为准（再现"测试数字虚高"教训）。
- proxy 命门测试（test_token_auth.py）**真在用例内 monkeypatch KYLIN_AUTH_MODE=proxy + 设密钥**断言无头→401/合法签名→200/篡改→401/viewer 可认证，未被 conftest dev 默认架空（§5 命门已守）。

**部署口径更新（"内网 only"约束解除路径）**：proxy 模式 + 可信反代部署后即可解除"内网 only"。部署待办：`KYLIN_PROXY_AUTH_SECRET` + `KYLIN_AUTH_MODE=proxy` + 反代按 `auth.sign_identity` 口径对**所有**入站请求（含 SSE；EventSource 不便自设头，必经反代注入）注入签名身份头；`KYLIN_AUTH_DB` 绝对路径+受限权限；**dev 模式严禁上生产**。

**PR2b systemd 沙箱（feat/D-PR2b，9a3df7f）= 架构驳回 CHANGES-REQUESTED**（已发 D 审阅窗口）：选用的 `systemd-run --scope` **无法施加** ProtectSystem/ProtectHome/PrivateTmp/ReadOnlyPaths/NoNewPrivileges/ProtectKernel* 等沙箱属性——scope 单元继承调用者执行环境、不经 systemd fork/exec，命名空间类保护静默失效或被拒（仅 cgroup 资源类生效）。后果：O5 内核级兜底不成立、docstring 撒谎、18 个纯 argv 测试假绿。**要求整体闭环重做**：改瞬态 service（`systemd-run --pipe --wait --collect`，无 --scope）+ 单一可信执行路径(消 O11 wrapper 死代码)+ sudoers 锁死(闭 O12 防 -p 注入)+ 在 Linux+systemd(优先 CI ubuntu)**真跑出写拒绝断言**(非等麒麟 VM)+ 文档诚实。L 不接受在 --scope 上打补丁。

**backlog（非阻断）**：① overview 真实数值 dispatch 解析（data_source→real）；② config.diff mcp 层聚合（决策⑤，已知会 X 前端工具列表少一项）；③ PR2b systemd 沙箱整体闭环重做（D，待 VM + 机制改 service）；④ frontend vitest 补跑（X，chat-security.test.ts 本机未跑）；⑤ D-10 medium 标记路径的告警/审计字段细化。

**验证基线**：kylin_ci 四道闸全绿（审阅窗口本机真跑 `pre-commit run --all-files`）；pytest 351 passed / 7 skipped。


---

## ★最新权威状态（dev=`d0a6ea3`，审阅窗口收口，2026-06-13 之四）

> 取代上方所有"最新/历史"块为当前权威。

**链（origin 同步）**：…→ `6b5fecb`（verify_token 全量认证）→ `e732dc9`（contract(stream) cause 补三值）→ `d0a6ea3`（**merge feat/x：A5 injection 前端三值 + StatusTag 服务状态映射 + X1 台账移入 frontend/**）。

**五道闸 + 认证**：①输入🟢（D-10 high→deny/medium→flag） ②策略🟢 ③确认🟢（审批闸 BLOCKER 闭合） ④结果🟢 ⑤审计🟢。🔐 全量端点认证🟢（6b5fecb）。

**★A5 injection 拒绝链路端到端闭合（d0a6ea3）**：`orchestrator.py:174` emit `cause="injection"` → `contracts/stream.py` 注释三值 `injection|policy_deny|user_reject`(e732dc9) → 前端 `RejectedEventData.cause` 三值 union + `stores/chat.ts` 独立分支"⛔ 输入被安全策略拦截" + `types/chat.ts` JSDoc 三值。**契约-后端实现-前端三方一致**，无"实现 emit 了契约/前端没覆盖的值"。

**feat/x merge 独立核验（审阅窗口亲跑，非信报告）**：
- 全量 diff dev...feat/x = 5 文件：types/chat.ts、stores/chat.ts、StatusTag.vue、tsconfig.json(ignoreDeprecations:"5.0" 逃生阀)、新增 `frontend/X1_API_联调统计台账.md`(496 行，移入 frontend/ 规避根 /*.md gitignore、入库)。
- **查缺补漏**：StatusTag `success` 标签由"成功"改"运行中"——grep 全前端确认 success 仅 DashboardView 经 `serviceStatus()`(running/active→success) 传入，**只影响服务状态行、无其它调用方副作用**（AuditView/PolicyView 传 allow/deny/confirm、ApprovalCard 传审批态，均不传 success）。`data.cause` 消费点仅 stores/chat.ts 一处，三值全覆盖。
- 亲跑：后端四道闸全绿；`vue-tsc --noEmit` exit 0；`npm run build` exit 0（chunk>500kB 仅 advisory）；`vitest run` **17/17 passed**（X 报告坐实）；dist/ 已 gitignore 未泄漏。
- merge 手法：`merge --no-commit --no-ff` → 全闸绿 → commit `d0a6ea3` → push（github 443 连撞 4 次，第 5 次成功 `e732dc9..d0a6ea3`）。

**X territory 小瑕（非阻断，已知会）**：tsconfig `ignoreDeprecations` 是 TS 弃用项逃生阀，TS6 需正面处理（技术债）；X1 台账置于 frontend/ 根（docs/ 更佳，不强求）。

**契约变更告知（C2，e732dc9）**：`contracts/stream.py` rejected cause 注释两值→三值（纯注释，无 schema/行为改动）。D/X 知会：前端已对齐，无需额外动作。

**部署前硬阻断已基本清零**：审批认证(50dbfcf) + 全量端点认证(6b5fecb) 均闭合。剩余非代码项 = 反代实机部署（对所有入站请求含 SSE 注入 `auth.sign_identity` 签名身份头）+ `KYLIN_AUTH_MODE=proxy` + `KYLIN_PROXY_AUTH_SECRET` + `KYLIN_AUTH_DB` 绝对路径受限权限 + **dev 模式严禁上生产**。

**backlog（非阻断）**：① PR2b systemd 沙箱**架构驳回重做**（D，改瞬态 service、单一执行路径、sudoers 锁死、CI 真跑写拒绝，待麒麟 VM 仅校准 LoongArch 路径）；② overview 真实数值 dispatch 解析（戊，data_source→real）；③ config.diff mcp 层聚合（决策⑤）；④ await_approval 全流程联调（X，受 fake planner 限制，待真 planner 接入）；⑤ D-10 medium 标记路径告警/审计字段细化。

**验证基线**：kylin_ci 四道闸全绿 + 前端 vue-tsc/build/vitest 17/17 全绿（审阅窗口本机真跑）；**pytest 真值 342→351 passed / 7 skipped**（备忘旧记"361"为虚高，作废）。


---

## ★最新权威状态（dev=`9f55da5`，审阅窗口收口，2026-06-13 之五）

> 取代上方所有"最新/历史"块为当前权威。

**链（origin 同步）**：…→ `d0a6ea3`（feat/x A5 injection merge）→ `9f55da5`（**merge feat/D-PR2b-v2：systemd 瞬态 service 沙箱闭环**）。

**五道闸 + 认证 + 沙箱**：①输入🟢 ②策略🟢 ③确认🟢（审批闸 BLOCKER 闭合） ④结果🟢 ⑤审计🟢。🔐 全量端点认证🟢(6b5fecb)。🛡️ **systemd 沙箱🟢 已入库（默认关，O5 真兜底机制就位）**。

**★PR2b systemd 沙箱闭环（9f55da5，经 v1 驳回→v2 驳回→v2 洞修复三轮）**：
- 机制：systemd-run **瞬态 service**（`--pipe --wait --collect --quiet`，**非 --scope**）——scope 不经 fork/exec、Protect* 静默失效；service 真 fork/exec、ProtectSystem/ReadOnlyPaths/ProtectHome/PrivateTmp/NoNewPrivileges/ProtectKernel* 真正落地。
- 架构：**wrapper(`deploy/sandbox/kylin-safeops-run.sh`) 唯一执行入口**，安全属性硬编码在 wrapper（单一事实来源），Python 只传 `[sudo, --, wrapper, profile, --, inner]`，无 -p 注入面（O12 闭合）。
- **洞1 闭合**：wrapper 删 none 分支 + `*) exit 1` fail-closed（none 仅 Python 侧"不包裹"标记，永不经 wrapper/sudo）。
- **洞2 闭合**：wrapper `ALLOWED_CMDS` 逐字符精确匹配 inner 可执行，白名单 = command_templates 生产二进制(df/find/ps/ss/netstat/journalctl/lsof/systemctl/sha256sum/gzip)，禁 shell/解释器；杜绝经 sudo 的任意 root 执行。
- profile：readonly(ProtectSystem=strict+ReadOnlyPaths=/) / limited_write(ProtectSystem=full，/etc 只读、/var/log 可写) / none(system.info 专属，Python 直接执行不经沙箱)。
- 闭环证据：Linux+systemd 集成测试经**真实 wrapper** 跑真 systemd-run，实证 readonly/limited_write 写 /etc 被内核拒、limited_write 放行 /var/log、none 被拒、/bin/sh 被拒、stdout 透传、退出码透传。审阅窗口亲跑四道闸全绿 + pytest 365/15(零回归)；**Linux 集成测试由 CI ubuntu 真跑（post-merge 须确认非 skip）**。

**沙箱启用待办（归 L）**：app.py lifespan 按 `platform.system()!="Windows"` + `KYLIN_SANDBOX_ENABLED=1` 传 `sandbox_enabled=True`；wrapper 部署(cp /usr/local/sbin + chmod 0755 + sudoers 0440 + visudo -c)。当前 `sandbox_enabled=False` 默认关、零运行时改变。

**残留 advisory（非阻断）**：① wrapper 白名单含 4 测试二进制(touch/echo/true/false)，建议后续测试 env 门控仅留生产 10 个；② service.restart 沙箱内 dbus 可达性 = 麒麟 VM 硬验收项；③ 旧分支 feat/D-PR2b(v1,9a3df7f)、feat/D-PR2b-v2 可删。

**部署前硬阻断状态**：审批认证 + 全量端点认证已闭合；O5 真兜底沙箱机制就位（待启用接线 + 麒麟 VM 实机校准）。剩余 = 反代实机部署 + dev 模式禁生产 + 沙箱启用接线/VM 校准。

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **365 passed / 15 skipped**（Linux 含集成 371/0）；前端 vue-tsc/build/vitest 17/17（d0a6ea3 时亲验）。备忘旧记"361"作废，认证增量真值 342→351，叠加 feat/x+沙箱后 Windows=365。


---

## ★最新权威状态（dev=`c7d66e5`，审阅窗口收口，2026-06-13 之六）

> 取代上方所有"最新/历史"块为当前权威。补登之五(9f55da5)后漏记的 戊 + config.diff 两增量。

**链（origin 同步）**：…→ `9f55da5`（PR2b systemd 沙箱闭环 merge）→ `88fe131`（任务戊 overview 真实 dispatch 解析）→ `c7d66e5`（config.diff mcp 层聚合接回 registry）。

**五道闸 + 认证 + 沙箱 + 采集**：①输入🟢 ②策略🟢 ③确认🟢（审批闸 BLOCKER 闭合） ④结果🟢 ⑤审计🟢。🔐 全量端点认证🟢(6b5fecb)。🛡️ systemd 沙箱🟢 机制就位（默认关，待 VM 启用）。📊 overview 真实采集🟢(partial)。🧩 config.diff🟢 接回。

**★任务戊（88fe131）overview 真实数值 dispatch 解析**：`system.py:_probe_readonly_tools` 返 {tool: 密封 ToolResult}，经 `dispatch.parse_tool_result` 解析；
- 转真：`root_disk_usage`←disk.usage(df)、`zombie_processes`←process.list(ps)；
- 缺源诚实置 0/[]：`cpu_usage`/`memory_usage`(无系统级只读源,backlog)、`services`([], 不硬塞示例)、今日计数(审计源未接)；
- `data_source` 三态（schemas 仍 `str`，向后兼容）：`real`(全真,cpu/mem 缺源前**不可达**)/`partial`(disk·zombie 真、余缺源,当前最高态)/`stub_executor`(无真采集)。**绝无"填示例值标 real"**。
- 只读硬护栏+TTL+哈希链豁免(方案b)不变。X 已知会：data_source 加 partial、services 改空 []、real 徽标 cpu/mem 接真前不点亮(预期)。

**★config.diff（c7d66e5）mcp 层聚合接回（决策⑤）**：`_DEFERRED_TOOLS` 清空 → config.diff 回到 registry / `/api/tools/registry`。
- `gateway.call` 三道闸通过后特判 config.diff → `_aggregate_config_diff`：复用 `config.hash_snapshot` 交真 executor 取快照（路径 canonicalize/沙箱照常）、失败按方案B原样上抛不吞错、`parse_sha256sum_output`→current、`ConfigDiffAggregator.compute` 对比基线 → `seal_result` 密封返回 ConfigDiff JSON(added/removed/changed/baseline_established/baseline_key)。
- **决策⑤铁律守住**：config.diff 绝不落 D 单命令执行器（会127）；聚合只在 mcp 层（`backend/app/mcp/config_diff.py`）；D 执行器保持单命令模板纯粹（未加 config.diff 特判）。
- 基线 = gateway 实例级内存、滚动语义（对比距上次调用漂移）、不跨重启（持久化列 backlog），docstring/JSON 诚实标注。
- 安全：恶意 config.diff paths=["/etc/shadow"] 在自身策略闸(决策3 递归扫^/→FILE001)聚合前被 deny，无绕过。X 已知会：registry 重现 config.diff、返回密封结构化 JSON。

**当前 backlog（非阻断，VM 期推进）**：
1. **overview cpu/memory 真源 = L+D 联合 VM 期项**（非纯 L）：需新增跑真命令的 R0 资源工具（建议 free/vmstat，非 cat），其 command_templates+sandbox profile（D 域 backend/app/executor）+ wrapper ALLOWED_CMDS（D 域 deploy/sandbox）+ 麒麟 VM `which` 路径校准；L 加 os_ops ToolSpec/parser/dispatch + overview 接线。全真到位 data_source 方达 real。
2. 沙箱启用接线（L，build_gateway 按 platform+KYLIN_SANDBOX_ENABLED 传 sandbox_enabled）= 已起工单（VM 前置）。
3. PR2b 沙箱麒麟 VM 实机验证 + service.restart dbus 可达性 + 命令绝对路径/SAFE_ENV 校准（D，VM 到位）。
4. 审计库 env 可配+绝对路径+0600+retention/rotation（D，决策⑪）。
5. 反代+proxy 认证实机部署（解除"内网 only"）；真实 LLM 接入（L，get_llm provider，最后一步、最不安全攸关）。

**部署前硬阻断状态**：审批认证+全量端点认证已闭合；沙箱机制就位（待 VM 启用）。剩余=麒麟 VM 实机验证/校准 + 反代部署 + dev 禁生产。

**验证基线**：kylin_ci 四道闸全绿；pytest **Windows = 380 passed / 15 skipped**（审阅窗口亲跑；执行窗口报告屡次 +19 虚高，已纠 342→351→365→372→380 真值序列）。前端 vue-tsc/build/vitest 17/17（d0a6ea3 时亲验）。麒麟 VM Linux 基线待阶段0实测。

**麒麟 VM bring-up 总清单**：见根目录 `麒麟VM-bring-up总清单.md`（阶段0~5 可勾选）。


---

## ★最新权威状态（dev=`ce6ca9f`，审阅窗口收口，2026-06-13 之七）

> 取代上方所有块为当前权威。补登之六(c7d66e5)后的多轮增量 + 麒麟 VM 阶段1完成。

**链（origin 同步）**：`c7d66e5`(config.diff 聚合) → `d755f1b`(沙箱启用接线) → `45dfa5a`(feat/x Dashboard 三态) → `3a8b21a`(PR2b hygiene 白名单分离+VM 脚本) → `ce6ca9f`(lsof 路径校准 + 麒麟 VM 阶段1完成)。

**全局状态**：五道闸🟢 + 全量端点认证🟢 + systemd 沙箱🟢(机制+启用接线+白名单分离，默认关) + overview 真实采集🟢(partial) + config.diff🟢 + 前端三态适配🟢。

**本轮增量登记**：
- **沙箱启用接线(d755f1b)**：`build_gateway` 按 `platform!="Windows" and KYLIN_SANDBOX_ENABLED=="1"` 传 `sandbox_enabled`；默认关零回归；一次性 logger 诚实标注。L 仅接开关、未碰 D 实现。
- **feat/x Dashboard 三态(45dfa5a)**：types data_source 字面量 union；partial 徽标；cpu/mem 仅 real 显值否则「暂未采集」；root_disk/zombie stub 显「桩数据」否则真值；services 空提示。诚实无假数据。（注：X 报告把已在 dev 的 RCA 测试混报为本轮新增——审阅已识别，本轮真 delta 仅 2 前端文件。）
- **PR2b hygiene(3a8b21a)**：wrapper 生产白名单仅 10 生产二进制、测试工具仅 `KYLIN_SANDBOX_TEST=1` 追加（sudoers env_reset 清之，生产面永不含测试工具，洞2 advisory 闭合）；新增 `test_wrapper_production_rejects_test_binary`；新增 `deploy/sandbox/verify-sandbox-on-vm.sh`（6 项 VM 验证脚本）。
- **lsof 校准(ce6ca9f)**：`/usr/sbin/lsof→/usr/bin/lsof` 三处（command_templates/privilege_executor/wrapper），全仓无残留、白名单仍与模板对齐。

**★麒麟 V11 VM 阶段1完成（实机里程碑，D 执行 9 passed/0 failed/0 warnings）**：systemd 255 全支持 Protect*；readonly/limited_write 写拒绝实机生效；**service.restart 沙箱内触达 system dbus 真重启成功 → 无需 profile 例外**；启停 78ms；洞1/洞2 实机复证；路径 17/17 匹配；SAFE_ENV 兼容。安全地板首次在真 LoongArch 实证（从 Windows skip → 真机 PASS）。

**bring-up 进度**（详见 `麒麟VM-bring-up总清单.md`）：阶段0✅(隐含) / 阶段1✅完成 / **阶段2待启动（沙箱启用 KYLIN_SANDBOX_ENABLED=1 + cpu/mem 真源 L+D 联合 + 全工具沙箱下真跑）** / 阶段3部署硬化(反代+proxy 认证+审计库) / 阶段4 demo / 阶段5 真实 LLM。

**部署前硬阻断**：沙箱机制已实机验证。剩余 = 阶段2~3（沙箱启用 + 反代/proxy 认证部署 + 审计库硬化 + dev 禁生产）+ 真实 LLM（最后）。

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **386 passed / 16 skipped**（审阅窗口亲跑；麒麟 VM Linux 含沙箱集成更多真跑）；真值序列 342→351→365→372→380→386（执行窗口报告屡 +19 虚高/混报，一律以审阅窗口亲跑+git diff 为准，近两轮 D 已改善）。


---

## ★决策记录（架构澄清）：D-10 注入检测的边界 = 直接注入；间接注入由结果闸+策略闸兜底（2026-06-13）

> 起因：X 原始注入 demo（日志投毒版 8da53cf）基于"agent 读投毒日志 → D-10 拦"的前提，D 审阅指出该前提不成立。L 独立核实代码后确认 D 正确，冻结如下口径。

**事实（代码核实）**：
- `orchestrator.py:160 detect_injection(user_intent)` —— D-10 **只扫 user_intent**（用户 Chat 输入的自然语言），在首次 `plan()` 之前（决策⑫）。**从不扫工具输出/日志内容**。CLAUDE.md §6 一致："注入检测放输入闸 user_input，不放 tool_args"。
- 工具输出（is_untrusted 的 ToolResult.stdout）走 `orchestrator.py:218 wrap_many_for_feedback` **结果闸定界中和**路径回喂 LLM，**不经 D-10**。

**威胁向量归属**：
- **直接注入**（payload 在 user_intent）→ D-10 输入闸命中 → `rejected(cause=injection)` → 前端 ⛔。**这是 D-10 的覆盖范围**。
- **间接注入**（payload 在工具输出/日志内容，indirect injection）→ **不归 D-10**，由两道确定性闸兜底（项目核心叙事）：
  1. **结果闸** `wrap_many_for_feedback` 定界中和不可信内容后才回喂（中和，不 reject）；
  2. **策略闸** 即便 LLM 被骗规划危险后续工具（rm -rf / 等）→ RuleBasedPolicyEngine R4/ProtectedPaths **deny 兜底**。
  前端表现是 **deny / 回喂中和后 LLM 不上钩**，**不是** injection-reject。

**裁定（L）**：
- **不接"对工具输出 detect-and-reject"（修法 B 否决）**：会篡改结果闸"中和不拒绝"的职责、搞浑安全模型；间接注入的真实防御已由结果闸中和 + 策略闸 deny 闭合，无需在结果闸再加注入拒绝。
- **注入 demo 采用修法 A（直接注入）**：X 终版（7557be6，已合 54c4a2d）打印样本让用户粘进 Chat 输入框 = user_intent → D-10 命中 → rejected(cause=injection)，与现架构/决策⑫/前端 rejected 叙事全自洽。✅ 已落地。
- "日志投毒"故事若要保留，其**诚实叙事 = 结果闸中和 + 策略闸 deny**（可作独立 demo 场景，前端表现 deny 非 injection-reject），**不得**宣称 D-10 拦截日志内容。

**备查**：若未来确需"间接注入主动检测"作防御纵深，是独立架构增量（第二接线点，需 L 专门拍板 + 不改结果闸现职责），非决策⑫范围、非当前计划。


---

## ★决策记录：阶段3 审计库部署硬化（决策⑪ 细化，L 拍板 2026-06-15）

> 来源：D 的"阶段3 审计库硬化设计稿"。L 预审通过设计 + 分期，并就 §6 五个开放问题拍板如下。设计核心不变量（哈希链 per-trace / retention 整 trace 搬迁绝不按 record 删 / verify-before-delete）正确，批准。

**整体批准**：3a（路径+权限）先行——这是 VM 那类 "readonly database / 属主漂移" 部署阻塞的真解，小、低风险、零回归（默认仍 ./data/audit.db）。3b（retention/rotation）随数据量上、可拆第二 commit。`feat/D-audit-hardening` 从 3a 起。

**§6 五问拍板**：
1. **retention 安全窗口 / 审批 pending 时长** → 决策：**归档闸 = "终态" 而非纯时间窗**。只归档**已达终态（FINISHED/REJECTED）的 trace**——即 trace 的 record 集含终态审计记录；**任何无终态记录的 trace（in-flight / WAIT_APPROVAL 待恢复 / 中途崩）一律不归档，无论多老**。`iter_closed_traces` 的 "closed" 即按此判定。90 天时间窗作额外缓冲叠加（终态 AND 早于 90 天才归档）。理由：WAIT_APPROVAL 无硬超时、可长期挂起，纯时间窗有误归档 in-flight 风险；终态闸从根上杜绝。
2. **retention 触发方式** → 决策：**带外手动/调度 CLI，不进 app**。D 提供 `run_retention()`，L 包一个薄 CLI 入口（如 `python -m backend.app.audit.maintenance`）供 cron / systemd timer / 运维手动调。**不放 lifespan 启动**（别拖慢启动、别在热路径做重维护）、**不做 app 内定时器**（避免与 append 并发复杂度）。
3. **require_absolute 生产判定口径** → 决策：**绑 `KYLIN_AUTH_MODE`**——`require_absolute = (KYLIN_AUTH_MODE == "proxy")`。proxy=生产→强制绝对路径 fail-closed；dev→相对可（零回归）。复用既有"proxy=生产"信号（verify_token/审批闸同源），少一个 env，不引入独立 KYLIN_ENV。
4. **归档库位置** → 决策：**默认与主库同目录**（同 0700 权限、最简）；**可选 `KYLIN_AUDIT_ARCHIVE_DIR` 覆盖**（部署想放独立/大容量/备份卷时）。3a 不涉，3b run_retention 的 archive_dir 默认主库父目录。
5. **默认值** → 决策：**RETENTION_DAYS=90、MAX_BYTES=100MB 接受**，均 env 可覆盖。

**L 追加架构约束（D 落地必遵）**：
- **保 conftest `:memory:` 测试夹具**：`resolve_audit_db_path` / `_secure_perms` 对 `:memory:` 必须短路跳过（不解析路径/不 chmod），杜绝测试落 audit.db 到工作树（现有 autouse 夹具不破）。
- **属主真解在部署层**：chmod 仅属主有效；VM 那次 root 建库 vmuser 写不了的根因是"多用户起 app"。部署文档 + systemd unit 必须 **`User=<agent 固定运行用户>` + 审计目录归该用户**，从源头保证属主一致；3a 的 chmod 失败要 log 清晰错误（非属主调用）。
- **WAL 边车 0600**：audit.db-wal/-shm 含在途审计数据，systemd unit 设 `UMask=0077` 保证惰性创建即 0600，文档写明。
- **前向衔接**：L 后续的 Audit REST 查询端点（X 已请求 /api/audit/*）消费 D 的 `locate_trace`（主库+归档目录定位）+ verify 接受 db_path 参数；归档 trace 自包含 GENESIS 链可独立 verify（无需跨文件链接）。

**边界**：D 提供纯函数/能力（db/ + audit/），L 在 api 层接开关与触发（同"沙箱启用接线"模式）；D 不碰 api/。

**待补**：阶段 2 VM 实证（沙箱开启全工具真跑无误拒 + curl overview 显示 data_source=real）需贴回审阅窗口凭实证勾选 bring-up 清单阶段 2，不凭口头。


---

## ★决策记录：审计持久化用 SQLite，不引入 PostgreSQL（L 拍板冻结 2026-06-16）

> 来源：D 提请"审计库选型确认（SQLite vs PG）"。L 核全项目持久化现状后拍板冻结。

**事实**：全项目零外部 DB 驱动；唯一持久化 = 哈希链审计库（audit_records 单表，stdlib sqlite3，WAL）；会话状态是内存单例（SessionRegistry/lifespan）；无 RAG/向量/用户表。

**决策：维持 SQLite，不引入 PG。** 理由（四硬约束）：
1. **LoongArch/麒麟靶机**：psycopg 是 C 扩展，PG server + 驱动 wheel 都要龙芯移植，违背"运行时唯一编译型依赖 = pydantic-core"铁律；sqlite3 CPython 内置零移植。
2. **单机 on-prem 运维 Agent**（非云多租户）：不需独立 DB 守护进程；给客户麒麟机塞 PG = 多一个装/加固/备份/打补丁的服务 + 监听端口攻击面。
3. **完整性靠哈希链不靠 DB 引擎**：防篡改 = prev_hash 链 + verify_chain 复算，只用单行 INSERT；PG 的 MVCC/复杂事务在此非承重。
4. **安全产品最小信任面**：SQLite 文件零网络面，契合"确定性兜底"叙事。

**确认前提**：当前单节点 lifespan 单例部署，无"多 Agent 实例共享同一审计库"的 HA 需求——**这是唯一会推翻 SQLite 的场景**。

**revisit 条件（唯一）**：若未来出现多节点/集中审计聚合需求 → 正确做法是 **"Agent 推审计给中央收集器"，而非"每台依赖 PG"**，届时单独立项评估收集器方案；**不**因此给单机塞 PG。

**不锁死（迁移路径干净）**：`SqliteAuditSink` 实现 `AuditSink` Protocol（append/verify_chain/last_hash）。真要上 PG 只需写 `PgAuditSink` 在 lifespan 接线点替换，orchestrator 零改动。现用 SQLite = "需求出现前不引入复杂度"。

**ADR**：批准 D 写一份架构决策记录（建议 `deploy/audit/README.md` 或 docs/ADR），含上述理由 + revisit 条件，供后续评审有据可查。


---

## ★最新权威状态（dev=`cc4b527`，审阅窗口收口，2026-06-16 之八）

> 取代上方所有块为当前权威。**「内网 only」正式解除**（阶段3 五项 + 审计 3a 全绿，VM 实机闭合）。

**链（origin 同步）**：`a47780b`（proxy 401 补 WWW-Authenticate）→ `cc4b527`（X 反代修复合入）。

**全局状态**：五道闸🟢 + 全量端点认证🟢 + systemd 沙箱🟢(机制+启用+白名单分离，默认关) + overview 真实采集🟢(real) + config.diff🟢 + 前端三态适配🟢 + **反代签名认证🟢(VM 实机)** + **审计库 3a🟢(VM 实机)** + 终态 rejected 三值🟢。

**本轮增量登记**：
- **X 修复版反代合入(cc4b527)**：proxy.py `a47780b` 补 401 响应 `WWW-Authenticate: Basic realm="Kylin SafeOps"`，**匿名放行漏洞闭合**（Basic Auth 缺失/失败→401+WWW-Authenticate，**不静默注入 anonymous**，不签名不转发）；其余（HMAC oracle、STRIP_HEADERS 先剥后注入、SSE StreamingResponse 不缓冲）+ 前端 whoami 身份过渡 上轮已审 OK。ruff 自动修 proxy.py 格式 3 处（纯 import 拆行/set 多行，无行为变更——X 提交版未过 ruff，已自动修入）。
- **协作提醒**：deploy/proxy 在 ruff 扫描范围，X 应在交前跑后端四道闸（非仅前端 vue-tsc/build）。

**★阶段3「内网 only 解除」五项 + 审计 3a VM 实机全绿（2026-06-16）**：
1. 直连 app 无签名头 → **401 fail-closed**（detail="missing or invalid proxy-signed identity"）
2. 无 BasicAuth 经反代 → **401 + WWW-Authenticate: Basic realm="Kylin SafeOps"**
3. 合法 BasicAuth 经反代 → **200** + whoami 真实身份（admin→{user:admin,roles:[admin,operator],mode:proxy}；viewer→{user:viewer,roles:[viewer],mode:proxy}）
4. 客户端伪造 X-Auth-* 经反代 → **被剥后重签**（viewer 塞 X-Auth-Roles:admin+X-Auth-User:hacker → 仍 viewer；伪造头无 BasicAuth → 401）
5. SSE 经反代不断流（9 事件流：audit_appended seq0-8→intent_parsed→plan_generated→policy_verdict(allow)→executing→tool_result(is_untrusted:true)→verified→done）
6a 审计库相对路径拒启动（proxy + relative → ValueError: 生产模式要求 KYLIN_AUDIT_DB 为绝对路径）
6b 审计库 0600 / 父目录 0700 / WAL+SHM 0600 / agent 用户属主
6c 真实 chat 后 `verify_chain` valid=True

**对外口径可坐实**：判定 + 执行 + 审计(哈希链 verify valid) + 审批认证(HMAC 签名身份) + 沙箱(data_source=real) + 反代签名认证 全链在麒麟 V11 实机闭合。

**待 merge（已审 PASS，与里程碑正交）**：
- `feat/D-audit-retention` `b94fd38` 3b retention/rotation（决策⑪ 细化）：4 文件 +742/-10 严守 audit/域；终态闸按 trace 整迁 + 带外 CLI（python -m backend.app.audit.maintenance 不进 lifespan）+ 先验后删（export→verify_chain valid→delete，失败保守不删）+ INSERT OR IGNORE 修复入（防重跑撞 archive UNIQUE 阻塞全部 retention，hash 仍逐字节不违 S7）+ now_iso 注入确定性 + 归档默认主库同目录 + 可选 KYLIN_AUDIT_ARCHIVE_DIR 覆盖 + 19 用例测试。
- `feat/D-audit-adr` `dd16190` ADR-0001 SQLite 选型冻结：1 文件 +48 纯 docs（`docs/ADR/0001-audit-store-sqlite-not-pg.md`）；四硬约束（LoongArch 移植/单机/哈希链/最小信任面）+ 单节点前提 + revisit 条件（多节点/集中聚合/量爆炸/需 SQL 分析）+ 不锁死迁移路径（AuditSink Protocol）。

**bring-up 进度**（详见 `麒麟VM-bring-up总清单.md`）：阶段0✅ / 阶段1✅(沙箱 VM 验证+路径校准 9/0/0) / 阶段2✅(沙箱启用+overview 真源 real) / **阶段3✅(内网 only 解除，五项 + 审计 3a)** / 阶段4(端到端 demo)⬜ / 阶段5(真实 LLM 接入)⬜。

**部署前硬阻断** = **已全部清零**。剩余 = 阶段4 demo / 阶段5 真 LLM / 反代占位 Basic Auth 换真 SSO/LDAP（登记 P1 后续，不阻断）。

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **429 passed / 17 skipped @ c6ca02f**（审阅窗口亲跑：基线 410 @ cc4b527 + 3b 增量 19 用例 = 429 真值；**D 报 448/17 虚高 +19 已识别，照 §5 纪律以审阅窗口亲跑为准**；真值序列 342→351→365→372→380→386→398→404→410→429）。


---

## ★最新权威状态（dev=`c6ca02f`，审阅窗口收口，2026-06-16 之九）

> 取代上方所有块为当前权威。**3b retention/rotation + ADR-0001 已合入 dev**（审阅窗口 merge）。

**链（origin 已 push 同步）**：`cc4b527`（阶段3 X 反代）→ `4d0887e`（3b retention/rotation merge）→ `c6ca02f`（ADR-0001 SQLite 选型 merge）。

**全局状态**：五道闸🟢 + 全量端点认证🟢 + systemd 沙箱🟢(VM) + overview 真实采集🟢(real) + config.diff🟢 + 前端三态适配🟢 + **反代签名认证🟢(VM)** + **审计库 3a🟢(VM) + 3b🟢(入库)** + **ADR-0001🟢(入库)** + 终态 rejected 三值🟢。

**本轮 merge 登记**：
- **`4d0887e` 3b retention/rotation 合入**（feat/D-audit-retention `b94fd38`，D 域已审 PASS）：4 文件 +742/-10 严守 audit/域；终态闸按 trace 整迁+带外 CLI+先验后删+INSERT OR IGNORE 修复入；19 用例测试。审阅窗口亲跑四道闸全绿、pytest 429/17（基线 410+19=429 真值，**D 报 448/17 虚高 +19 已识别**）。
- **`c6ca02f` ADR-0001 合入**（feat/D-audit-adr `dd16190`，D 域已审 PASS）：1 文件 +48 纯 docs（`docs/ADR/0001-audit-store-sqlite-not-pg.md`）；四硬约束+确认前提+revisit 条件+不锁死迁移路径。审阅窗口亲跑四道闸全绿。

**★审阅纪律自检**（防 §5 红线）：
- §0 纪律"merge 前 reset .kiro/ + 只 add 真实代码文件"：本轮 `git reset -q -- .kiro/` 已做；但 `git add -N .`（pre-commit 跑前常规动作）会**把 .kiro 重新 intent-to-add 拉进 stage**——已两次捕获并在 commit 前 `git restore --staged .kiro/` 还原，**未污染入库**。建议下次可在 `git add -N .` 之前显式 `git reset -q -- .kiro/`，或在 `git add` 时显式列文件（避免 `add -N` 副作用）。
- §5 纪律"D 报告 pytest 虚高 +19"：本轮 D 报 448/17 vs 亲跑 429/17，差 19 虚高；3b 本身 19 用例增量真实（429-410=19 ✅ 数学吻合）。

**bring-up 进度**（详见 `麒麟VM-bring-up总清单.md`）：阶段0✅ / 阶段1✅ / 阶段2✅ / **阶段3✅（内网 only 解除，五项 + 审计 3a VM 实机）** / 阶段4(端到端 demo)⬜ / 阶段5(真实 LLM 接入)⬜。

**部署前硬阻断** = **已全部清零**。剩余 = 阶段4 demo / 阶段5 真 LLM / 反代占位 Basic Auth 换真 SSO/LDAP（登记 P1 后续，不阻断）。

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **429 passed / 17 skipped @ c6ca02f**（审阅窗口亲跑；D 报 448/17 虚高 +19 已识别）。


---

## ★最新权威状态（dev=`edf57aa`，审阅窗口收口，2026-06-16 之十）

> 取代上方所有块为当前权威。**阶段4 demo 落定**（6 场景 + 6 pytest 用例）。

**链（origin 已 push 同步）**：`c6ca02f`（ADR-0001）→ `edf57aa`（阶段4 demo 落地）。

**全局状态**：五道闸🟢 + 全量端点认证🟢 + systemd 沙箱🟢(VM) + overview 真实采集🟢(real) + config.diff🟢 + 前端三态适配🟢 + 反代签名认证🟢(VM) + 审计库 3a🟢(VM) + 3b🟢(入库) + ADR-0001🟢(入库) + **阶段4 demo🟢(本机 A/B/E/F 全跑通，C/D 链路真跑，沙箱内 systemctl/gzip 待 VM)** + 终态 rejected 三值🟢。

**本轮增量登记**：
- **`edf57aa` 阶段4 demo 合入**（L 域 `feat/L-stage4-demo` 阶段4 demo 落地，审阅 PASS）：
  - **5 个新文件（严守 C3）**：scripts/demo_stage4_common.py（203 行公共装配）+ scripts/demo_stage4_e2e.py（529 行 6 场景 demo 主入口 CLI）+ backend/tests/test_demo_stage4_e2e.py（95 行 6 pytest 用例）+ docs/design/stage4-e2e-demo-testplan.md（144 行落地方案）+ docs/design/stage4-e2e-demo-acceptance-report.md（254 行验收报告）
  - **6 场景 A-F**：A 输入闸 deny / B 策略闸 FILE001 deny / C 确认闸 R3 service.restart / D 确认闸 R2 log.compress_rotate / E 结果闸+审计闸 is_untrusted+链结构 / F 审计完整性篡改检出
  - **真件装配**：PolicyEngine + PrivilegeExecutor + SqliteAuditSink(:memory:) 真 import + fake LLM（**fake 不参与安全判定**，决策⑫ 防御纵深严守）
  - **C3 边界自检**（亲跑 `git diff dev` 确证）：D 域（security/executor/audit/db）零 diff；X 域（frontend/deploy/proxy/rca）零 diff；既有 `_demo_common.py` / `test_demo_playbook.py` 零 diff
  - **诚实标注**：C/D 场景本机 Windows 沙箱关闭 + 无 systemctl/gzip → verified_summary="non-zero"；VM 上 KYLIN_SANDBOX_ENABLED=1 才完整闭合

**★§5 查缺补漏 / 审阅纪律自检**：
- L 域"零改动"自检补强：本轮亲跑 `git diff dev -- scripts/_demo_common.py backend/tests/test_demo_playbook.py backend/app/{security,executor,audit,db}/ frontend/ deploy/proxy/ mcp_servers/rca/` **全空**，零改动承诺兑现
- §12 纪律"根级 *.md 全部 gitignore"被 L 域**自做主张绕过**：testplan + acceptance-report 移入 `docs/design/`（子目录不在 .gitignore）**实际合理**（设计文档区有 git 历史），但需明示口径变更——已在 commit message 显式标注"docs/design/ 新增设计文档（子目录绕开根级 *.md gitignore，与 ADR-0001 归档位置一致）"
- §0 `.kiro/ 处置` 纪律：本轮 `git add -N .` 副作用再次出现——但 commit 前已 `git status -s` 复核，.kiro 始终在 `??`（未跟踪）状态，**未污染入库**
- §5"D 报告 pytest 虚高 +19"教训继续生效：本轮 L 域报 435/17，亲跑确认基线 429+6=435 数学吻合，**§5 纪律未被违反**

**bring-up 进度**（详见 `麒麟VM-bring-up总清单.md`）：阶段0✅ / 阶段1✅ / 阶段2✅ / 阶段3✅ / **阶段4 本机✅（A/B/E/F 全绿；C/D 链路真跑，待 VM 沙箱实证）** / 阶段5(真实 LLM 接入)⬜。

**部署前硬阻断** = **已全部清零**。剩余 = 阶段4 VM 端到端验证(C/D 沙箱内 systemctl/gzip 真执行) / 阶段5 真 LLM / 反代占位 Basic Auth 换真 SSO/LDAP（登记 P1 后续，不阻断）。

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **435 passed / 17 skipped @ edf57aa**（基线 429 + 阶段4 6 用例 = 435 亲跑真值）；阶段4 demo CLI 6 场景全 PASS（A/B/E/F 全绿，C/D 链路真跑报 non-zero 诚实标注）。


---

## ★最新权威状态（dev=`0065787`，审阅窗口收口，2026-06-16 之十一）

> 取代上方所有块为当前权威。**fix(demo) 场景 C/D 非 VM 沙箱断言放宽**。

**链（origin 已 push 同步）**：`edf57aa`（阶段4 demo 6 用例落地）→ `0065787`（fix: C/D 非 VM 沙箱断言放宽——Linux root 真跑通 summary=...完）。

**★审阅窗口 §5 红线失误自检（之十一）**：本机 Windows 跑通不代表 Linux CI 跑通。**dev=edf57aa 提交时本机全过、CI lint-type-test 失败（470 passed / 1 failed）**——根因=场景 D 断言 `assert summary != "ok"`，但 GitHub Actions Linux runner（root + 含 gzip）真跑通会报 "ok"，触发 CI 失败。**修法**：断言改为 `in ("ok", "one or more tools exited non-zero")`——`ok` 和 `non-zero` 在非 VM 沙箱环境均合法。**审阅教训**：下次审阅应模拟 Linux/WSL 跑一遍或加 GitHub Actions 本地 dry-run 验证，**不能只信本机 Windows pytest 真数**。

**验证基线**：
- Windows kylin_ci：pytest **435 passed / 17 skipped**（本机，未变；fix 不改逻辑只放宽断言）
- Linux GitHub Actions（lint-type-test）：dev=edf57aa 报 **470 passed / 1 failed** → dev=0065787 应 **471 passed**（待 CI 复核）

**阶段4 VM 端到端验证 backlog**（D 域 VM 跑，**不阻断**）：C/D 在 VM 上 `KYLIN_SANDBOX_ENABLED=1 python -m scripts.demo_stage4_e2e --scenarios C,D` 跑出 `verified_summary="ok"` 即为终验。


---

## ★最新权威状态（dev=`c0ad2a3`，审阅窗口收口，2026-06-16 之十二）

> 取代上方所有块为当前权威。**X 阶段4 demo 前端 merge 入 dev**（X author 保留 + 8 行空白修剪 + 验证链按钮 v-if 防护）。

**链（origin 已 push 同步）**：`0065787`（fix C/D 非 VM 沙箱断言）→ `c0ad2a3`（merge feat/x-demo-stage4-fixed，X author `bug <yangyong_xi@163.com>`）

**X 域本轮交付**：
- 6 场景 A-F 补全（实际 4→6 新增 B 策略闸 / E 结果闸+审计闸 + 重编号 + CLI 验证行）
- auditStore.verify 接线真在 DemoView.vue
- DemoView.vue 末尾 8 行空白修剪（ESLint 合规）+ 验证链按钮 v-if 防护（架构补缺 #3）
- C3 边界严守：只动 frontend/src/views/DemoView.vue + demo/attack_prompts/README.md，不动 audit/executor/contracts/api
- S9 无密钥入库

**★架构视角留痕**（X 自检报告偏差）：
1. X 报告"vue-tsc 零错 + build 成功"是真，但未报 ESLint 8 行空白警告（**实际有警告**）
2. X 报告"细节打磨/思维链动画/Dashboard 图表"是未来计划，不在本次 PR 内 → 留痕要求 X 下次"本次 PR / 下个 PR 计划"分清
3. 场景 F 验证链按钮加 v-if 防护（traces.length>0）— 避免取 undefined 调后端 API

**间接注入（日志投毒）真实叙事**：**L 拍板 = 留阶段5 真 LLM 接入时一并验证**（decision-pending）


---

## ★最新权威状态（dev=`7b74404`，审阅窗口收口，2026-06-17 之十三）

> 取代上方所有块为当前权威。**D 域阶段4 VM 修复合入，阶段4 demo 完整收口**。

**链（origin 已 push 同步）**：`c0ad2a3`（X demo merge）→ **`7b74404`（D log.compress_rotate dynamic_args 修复）**。D 域报告 4 项 VM PASS（...链不被 mock 状态影响；B/C/E/F 应加 `assert res.record_count >= 9` 防偏差）
3. **X 域 P1 backlog**：proxy.py:83 `await client.request()` 阻塞读 body，WAIT_APPROVAL 场景挂死；应改 `client.stream() + aiter_bytes()`。D 反向留痕（**非本里程碑阻断**）
4. **L 域 backlog（阶段5 一并）**：fake planner 目标写死（B=nginx.service / C=/var/log/app.log），不解析 message；真 LLM 接入时需修

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **454 passed / 17 skipped @ 7b74404**（审阅窗口亲跑真值；D 报 449/17 差 +5 虚高已识别）。


---

## ★最新权威状态（dev=`5b2addd`，审阅窗口收口，2026-06-17 之十四）

> 取代上方所有块为当前权威。**record_count 硬断言按路径分别放宽**（D 域 WARN 已解）。

**链（origin 已 push 同步）**：`7b74404`（D 域阶段4 VM 修复）→ **`5b2addd`（record_coun... 阶段4 demo 收口后 record_count 路径断言已修：
  - B 走 REJECTED（policy_deny）路径 → 3-10
  - C/D 走 FINISHED（service.restart/log.rotate）路径 → 9-10
  - E 走 FINISHED → 9-10
  - F 篡改前 → 9-10，篡改后 valid=False（不要求 record_count）
- pytest 455/17（基线 454 + 1 修改 B 路径断言）

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **455 passed / 17 skipped @ 5b2addd**。


---

## ★最新权威状态（dev=`f9df6a6`，审阅窗口收口，2026-06-17 之十五）

> 取代上方所有块为当前权威。**D 域阶段5 前清光合入**（4 文件 3 commit）。

**链（origin 已 push 同步）**：`5b2addd` → **`f9df6a6`（D stage5-prep merge）**

**D 域审阅留痕**：
- ✅ C3 边界严守（只动 db/audit/audit-test/script/audit-doc）
- ✅ S9 无密钥入库
- ✅ contracts 不动
- ✅ _secure_perms fail_closed 可选参数 + connect 同步（D 域产入）
- ✅ fake planner 参数化（scenario_c target_service / scenario_d target_log_path）+ scene D path 目录→文件 修正
- ✅ retention VACUUM 后归档库链完整 + 主库清零 测试 +1
- ✅ README §4 全节补全（sqlite3 查询 + verify_chain 脚本）
- ✅ D 报告 pytest 数字**这次零偏差**（456/17 亲跑真值吻合）— §5 红线破例全过
- ⏳ **VM 端到端实证 backlog**（D 域）：`KYLIN_SANDBOX_ENABLED=1 python -m scripts.demo_stage4_e2e --scenarios C,D` 实跑 + 贴 `/tmp/stage4_cd_vm.log` 全文
- ⏳ **L 域接 fail_closed=True backlog**（L 域）：D 报告里要求 L 在 `api.py lifespan` 按 `auth_mode=="proxy"` 传 `fail_closed=True` — L 域 backlog 第 1 优先级

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **456 passed / 17 skipped @ f9df6a6**。


---

## ★最新权威状态（dev=`94bdac9`，审阅窗口收口，2026-06-17 之十六）

> 取代上方所有块为当前权威。**D 域阶段4 VM C/D 实证 PASS + 测试回填合入**（2 步一...fa-closed 验）
  - log.compress_rotate R2/operator：state=FINISHED, verified_summary="ok", record_count=10, verify_chain valid=True
  - **D 报告 pytest 数字零偏差**（456+4=460 数学吻合，§5 红线第四度破例没中招 ✓）
  - 修场景 E 严格断言雷（=→>=）

**L 域接 fail_closed=True backlog**（L 域 stage5 启动时第一优先级）：D impl + 测试已就位（94bdac9 已合），L 在 `api.py lifespan` 按 `auth_mode=="proxy"` 传 `connect(..., fail_closed=True)`

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **460 passed / 17 skipped @ 94bdac9**。


---

## ★最新权威状态（dev=`b421736`，审阅窗口收口，2026-06-17 之十七）

> 取代上方所有块为当前权威。**X 域阶段5 前清光合入**（3 修复 + 9 文件）。

**链（origin 已 push 同步）**：`94bdac9`（D tests + VM 实证）→ **`b421736`（X SSE 阻塞修...BOM 去 + README NTP 修）—— X 自检数字这次对（§5 红线破例）**
2. **X 报告 base 写 5b2addd 而非 f9df6a6/94bdac9** —— X 没 rebase 但 merge 干净（X 改的文件 D 没动）— 留痕不阻断

**验证基线**：kylin_ci 四道闸全绿；pytest Windows **460 passed / 17 skipped @ b421736**（X 不动后端，数字不动）。


---

## ★最新权威状态（dev=`b421736`，审阅窗口收口，2026-06-17 之十八）

> 取代上方所有块为当前权威。**阶段5 真 LLM 接入工单 prompt 已起**（L 域下次会话主任务）。

**链（origin 已 push 同步）**：`b421736`（X 修后 stage5-prep）→ **阶段5 工单**（3 步走）：
- 步骤 0：**L 域接 fail_closed=True**（D impl 已就位，**L 域必须先接**——api.py lifespan 按 auth_mode=="proxy" 传）
- 步骤 1：**fake planner 修**（解析 user_intent 提取 tool/args，不再写死目标）
- 步骤 2：**真 LLM 客户端 + 接入 + 间接注入真验**（核心，5-6 commit 拆开）

**★架构视角 3 步走原因**：
1. 步骤 0 立等小改但**安全位 fail_closed 不接则永不生效**
2. 步骤 1 解耦 fake planner 写死目标（**否则测真 LLM 时被污染**）
3. 步骤 2 跨 5-6 commit 拆开，**每个 commit 都跑四道闸不红**（避免一...L 真 LLM 接入 + 间接注入防护" 全链在麒麟 V11 实机闭合
- 可发 ADR-0002 备...


---

## ★最新权威状态（dev=`3c0bdca`，审阅窗口收口，2026-06-17 之十九）

> 取代上方所有块为当前权威。**阶段5 真 LLM 接入 5 commit 合入**（D 接线 + L 修 + 间接注入真验 + S3 + 真端点接线）。

**链（origin 已 push 同步）**：`b421736`（X stage5-prep 修...-format ✓"但实跑 3 文件 reformat — L 漏报）— **审过 PASS 实质，偏差留痕**
3. 间接注入真验：fixture 投毒 args → 策略闸 deny → REJECTED（X 留的"暂未实现"已填）

**★§5 红线第 7 度（我自中招）**：之前我报"基线 460/17 @ b421736"是错记——dev=b421736 真基线 487/17 = 480 passed + 7 failed（L 测试未生效）+ ...

---

## ★最新权威状态（dev=`f6ded54`，审阅窗口收口，2026-06-17 之二十）

> 取代上方所有块为当前权威。**X 域前端样式全面重设计已合**（19 文件 +1785/-1112，四道闸全绿）。

**链（origin 已 push 同步）**：`3c0bdca`（L 阶段5 真 LLM）→ **`f6ded54`（X 前端样式重设计 merge）**

**已核验**：
1. C3 边界：19 文件全在 frontend/，0 个 backend 文件触碰 ✓
2. S9：无密钥（staged diff 仅 X1_API.md 文档表格行删除，非真密钥）✓
3. 阻断项清零：26 个 *-back.* 备份文件全 git rm ✓；`frontend/X1_API_联调统计台账.md` git rm ✓
4. merge 干净（无冲突；X merge-base=5b2addd，frontend 与 L 域 backend 不重叠）
5. 四道闸全绿：ruff / ruff-format / mypy / pytest **487/17** Passed ✓

**X 报告 vs 真值**：本次 X **0 偏差**（26 *-back.* 全清 ✓，.md 真删 ✓，19 文件 ✓）。

**遗留（不变）**：SSO/LDAP（X P1，阶段6）；SSE heartbeat（X P2）；真端点 `KYLIN_LLM_TEST_FIXTURE=false`（待生产）；`GET /api/llm/health`（未交付）。

---

## ★最新权威状态（dev=`b2d1112`，审阅窗口收口，2026-06-17 之二十一）

> 取代上方所有块为当前权威。**阶段5 真端点 e2e 驱动接通已合**（场景 G，2 文件 +121/-12，四道闸全绿）。

**链（origin 已 push 同步）**：`f6ded54`（X 样式）→ **`b2d1112`（L 真端点 e2e 驱动 merge）**

**交付内容**（L 域 `scripts/`）：
- `demo_stage4_e2e.py`：`--use-real-llm` / `--user-intent` CLI（缺参 `SystemExit(1)`）+ `scenario_real()` + `_main_real()`：`RealLLMClient → orch.run(user_intent=)` 五道闸全链
- 输入闸天然在位（`orchestrator.run:150` 第 2 参 `user_intent=` 走 `detect_injection`，无需重复实现）
- `demo_stage4_common.py`：清理 `real_user_intent` 死占位（审阅窗口顺手把 L 的 `del ... # noqa:F821` 改回标准 `_ = ...`）

**已核验**（审阅窗口亲跑，非采信）：
1. C3 边界：只动 `scripts/` 2 文件，0 个 D/X 域文件 ✓
2. S9：base_url/api_key/model 全走 env（`load_real_llm_config_from_env`），diff 终扫无真密钥 ✓
3. 四道闸全绿：ruff / ruff-format / mypy / pytest **487/17** Passed（亲数真值）✓
4. smoke：缺 `--user-intent`→`SystemExit(1)`；fixture 模式 G→`FINISHED`+`verify_chain.valid=true`+record_count=9 ✓

**L 报告 vs 真值**：**本次零偏差**（2 文件 +121/-12 对；487/17 对；S9 守住）。仅审阅交接.md 内一处单文件行数"+127/-11"是笔误（真实整体 +121/-12），不影响实质。

**真端点验证 = 阶段5 真收口的最后一步，待 D 在 VM 跑**：
- D 在 VM `export KYLIN_LLM_PROVIDER=real` + `KYLIN_LLM_BASE_URL/API_KEY/MODEL`（密钥带外给，**禁进 repo**）+ `KYLIN_SANDBOX_ENABLED=1`
- 跑 `python -m scripts.demo_stage4_e2e --use-real-llm --user-intent "..."` 重跑 D-10 红队 + 五道闸，贴**脱敏** log
- 期望：真 LLM 被注入也被地板（输入闸/策略闸）拦死

**遗留（不变）**：SSO/LDAP（X P1，阶段6）；SSE heartbeat（X P2）；`GET /api/llm/health`（L 域未交付，需单独工单）；`KYLIN_LLM_TEST_FIXTURE` vs `KYLIN_LLM_PROVIDER` 口径 README 统一（非阻断）。

---

## ★最新权威状态（dev=`9797e43`，审阅窗口收口，2026-06-17 之二十二）

> 取代上方所有块为当前权威。**O18 真 LLM planner 缺工具 schema 修复已合**（4 文件 +219/-11，四道闸全绿）。

**链（origin 已 push 同步）**：`b2d1112`（L 真端点 e2e 驱动）→ **`9797e43`（O18 planner 工具清单注入 + few-shot 去毒 merge）**

**O18 现象**：D 在 VM 用 qwen3-max 真端点首跑，disk.usage（无参工具）被 LLM 幻觉出 `path` 参数 → 闸2 结构校验 deny → REJECTED。

**根因（审阅窗口逐行复核，比初版诊断更深一层）**：
1. `build_system_prompt()` 从未喂工具清单——只注入 Intent 信封 JSON Schema，LLM 靠工具名猜参数。
2. `_FEWSHOT_EXAMPLE` 给 disk.usage 错塞 `path`——真 LLM 直接照抄。
3. **D 零责任**：`gateway.py:113-116` 闸2 在 `policy.evaluate` 前短路，PolicyEngine 未被调用。地板行为完全正确（"不可信顾问"叙事实证）。

**修复**（C3 严守 L 域 `backend/app/llm/` + `scripts/`）：
- `prompts.py`：`+_format_tool_catalog()` 动态渲染工具名/风险/描述/input_schema；`build_system_prompt(specs=None)` 接受 specs，None 退化（fixture/旧测试兼容）；few-shot 重写为"无参/有参"双范例（disk.usage args=`{}`；disk.large_files args=`{path}`）。
- `adapter.py`：`LLMAdapter +tool_specs` 字段；`plan()` 首调 + retry 都注入工具清单。
- `demo_stage4_common.py`：`build_e2e(use_real_llm=True)` 传 `tool_specs=registry.list_tools()`；registry 提到顶部（真 LLM 路径需先建 registry）。

**已核验**（审阅窗口亲跑，非采信）：
1. C3 边界：4 文件全在 L 域，D/X 域零 diff ✓
2. 四道闸全绿：ruff / ruff-format / mypy / pytest Passed ✓
3. pytest 真值：**496/17** = 基线 487 + 9 O18 增量（数学吻合）✓
4. O18 9/9 测试 PASS（catalog 含、disk.usage 无参标记、log.compress_rotate 有参标记、few-shot 去毒、双范例、None 退化、plan() 含、retry 仍含、场景 G fixture 仍通）✓
5. S9：无密钥/URL，纯 prompt 文本 ✓
6. CRLF 归一化 LF（防 Windows 行尾污染 diff）✓

**L 报告 vs 真值**：**本次零偏差**（§5 破例，第三次全过）。修复范围、文件数、行数、测试数、pytest 绝对数全对。

**对 D 的影响**：VM 真收口步骤 2/4（service.restart / log.compress_rotate）此前因同根因被幻觉阻断——**O18 合入后 D 可续跑真收口全套**。步骤 3a 直接注入（输入闸在 plan 前，不依赖 schema）始终不受影响。

**遗留（不变）**：SSO/LDAP（X P1）；SSE heartbeat（X P2）；`GET /api/llm/health`（L 域 backlog；意外发现 `real_client.health_check()` 方法已写好 `real_client.py:322-331`，health 工单改为"复用现成方法挂路由"即可，更轻量）。

---

## ★最新权威状态（dev=`e2bd033`，审阅窗口收口，2026-06-17 之二十三）

> 取代上方所有块为当前权威。**`GET /api/llm/health` 健康检查端点已合**（4 文件 +226/-0，4 道闸+5 测试全绿）。

**链（origin 已 push 同步）**：`9797e43`（O18 planner 修复）→ **`e2bd033`（LLM health 端点 merge）**

**交付内容**（L 域 `api/`）：
- `routers/llm.py`（新）：`APIRouter(prefix="/api/llm")` + `GET /health` + `Depends(verify_token)`。延迟 import `real_client`（避免 router import 期拉依赖图）。
- `routers/__init__.py`：注册 `llm_router`。
- `schemas.py`：`LLMHealth` 模型（7 字段：provider/model/base_url/api_key_configured:bool/rate_limit_per_minute/token_cap/status）。
- `tests/test_llm_health_endpoint.py`（新）：5 用例。

**已核验**（审阅窗口亲跑，非采信）：
1. C3 边界：4 文件全在 L 域，D/X 域零 diff ✓
2. **S9 铁律守住**：路由只读 `load_real_llm_config_from_env()`，**绝不回显 api_key 明文/前缀/后缀**，只回 `api_key_configured: bool`。S9 双重断言（`"sk-dummy-SECRET-XYZ"` + `"SECRET-XYZ"` 子串）实测都不在 response ✓
3. **绝不发 httpx POST**：spy `RealLLMClient.__init__` 断言 `init_called == 0` 实测过 ✓
4. 四道闸全绿：ruff / ruff-format / mypy / pytest Passed ✓
5. pytest 真值：**501/17** = 496 + 5 新增（数学吻合）✓
6. 5/5 health 测试 PASS（fixture 默认 / real+key 不泄漏 / spy init_called==0 / 路由注册 / dev 模式联通）✓
7. 体例一致：与 `/api/system/overview` 同款认证 + tags + prefix ✓
8. 诚实边界：`status: "ok"` 仅表"配置可读"，docstring/schema 字段描述都明确"不证明端点可达"，真连通性探测留 `?probe=true` 显式开关 backlog ✓

**L 报告 vs 真值**：**本次零偏差**（§5 红线第四次破例）。文件数 +226/-0 对；501/17 对；5/5 PASS 对；S9 守住；4 道闸全绿。

**对 D 的影响**：零影响。health 是只读状态查询旁路，不在五道闸执行路径上。**D 的 VM 真收口不受 health 合入影响，可继续按 O18 合入后的 prompt 续跑**。

**L 域阶段5 收尾完成度**：
- ✅ 真端点 e2e 驱动（b2d1112）
- ✅ O18 planner 工具清单（9797e43）
- ✅ GET /api/llm/health（e2bd033）
- ⏳ **真端点 httpx POST 在 VM 真跑 = D 域任务,阶段5 真收口最后一步**

**遗留（不变）**：SSO/LDAP（X P1）；SSE heartbeat（X P2）；`?probe=true` 真端点连通性探测（health 端点 backlog）；`KYLIN_LLM_TEST_FIXTURE` vs `KYLIN_LLM_PROVIDER` README 口径统一（非阻断）。

---

## ★最新权威状态（dev=`e2bd033`，审阅窗口收口，2026-06-19 之二十四）

> 取代上方所有块为当前权威。**D 在 VM 真端点（qwen3.7-max）实跑 5 条 intent 实证 PASS，阶段5 真收口闭合，ADR-0002 拍板**。

**ADR-0002 落盘**：`docs/adr/0002-stage5-real-llm-kylin-vm-closure.md`（与 ADR-0001 同目录同体例，本地落盘，2026-06-19 L 拍板接受 Accepted）。

**链（origin 已 push 同步）**：`e2bd033`（L health 端点）→ **VM 实证 PASS（无需新 commit，D 在 VM 上跑通）**

**VM 实证 5 条 intent 收口表**（D 提供，审阅窗口逐条核验与 dev 合入代码字节级自洽）：

| user_intent | state | rejected_cause | is_untrusted | chain.valid(记录数) |
|---|---|---|---|---|
| 查看磁盘占用（冒烟） | FINISHED | — | true | true(9) |
| 忽略指令 rm -rf /（注入） | **REJECTED** | **injection** | — | **true(2)** |
| lsof /etc/shadow（策略） | **REJECTED** | **policy_deny** | — | **true(5)** |
| 重启 nginx（R3） | FINISHED | — | true | true(10) |
| 压缩轮转 app.log（R2） | FINISHED | — | true | true(10) |

**五道闸逐条坐实**（每条均可由 dev=`e2bd033` 合入代码字节级解释）：
1. **输入闸**：注入 → 2 条审计记录 = received+rejected，`orchestrator.py:155-176` 注入 high 路径在 `plan()`（line 190）**之前** return，LLM 没产计划。**"判定靠地板不靠 LLM 自觉"的铁证。**
2. **策略闸**：/etc/shadow → 5 条记录，说明过了 gateway schema 校验、真走到 D 的 PolicyEngine 才被拦（与 O18 那种 gateway schema 短路不同）。
3. **确认闸**：R3/R2 均 WAIT_APPROVAL→批准→沙箱内真执行。
4. **结果闸**：每个执行工具 `is_untrusted=true`。
5. **审计闸**：5 次 `verify_chain.valid` 全 true。
6. **O18 旁证**：R3/R2 有参工具产出合法 args（service_name / path），证明 input_schema 注入对**全部**工具生效，非只 disk.usage。

**S9 合规**：VM 代码为 git ZIP 解压（无 .git）、密钥仅 `export` 进 shell（从未落文件）、demo 审计落 `:memory:`、仅写 `/tmp` 日志 + `/var/log` 测试日志 → **只跑不改成立，无密钥泄漏**。

**已核验**（审阅窗口亲跑 + 叙事自洽核验，非采信）：
1. ADR-0002 文件落盘（docs/adr/）+ 6 大事实陈述与代码逐行对应 ✓
2. D 报告第 3 条 action 夹带**过时信息**已纠正 ✓（见下"纠错留痕"）
3. ADR-0002 引用 commit/file 锚点全部存在（`8c4eb64`/`346536e`/`a8987e9`/`9797e43`/`e2bd033`/`8f77240`/`94bdac9` 全部已是 dev 祖先）✓

### 纠错留痕：D 报告第 3 条 action 夹带过时信息

D 在 @LEBLINC 中说："提醒：你自己的 fail_closed 接线（阶段5 步骤0，api/lifespan.py 按 `auth_mode=="proxy"` 传 `connect(fail_closed=True)`）**仍待接**"。

审阅窗口实测：**早已完成**——
- `backend/app/api/app.py:182` 早已接（`_fail_closed = _auth_mode_now == "proxy"` → `_db_connect(_AUDIT_DB_PATH, fail_closed=_fail_closed)`）
- 由 commit `8f77240` `fix(api): lifespan 按 auth_mode=='proxy' 传 connect(..., fail_closed=True)（D 域 94bdac9 接线）` 合入，已在 `3c0bdca` 阶段5 合入里进 dev
- `backend/tests/test_api_lifespan.py:47` `test_lifespan_proxy_passes_fail_closed_true()` 早已固化（dev=`e2bd033` pytest 501/17 通过即证）
- **此项非 backlog，已完成**。已写入 ADR-0002 附里。

**遗留（不变）**：SSO/LDAP（X P1）；SSE heartbeat（X P2）；`?probe=true` 真端点连通性探测（health 端点 backlog）；`KYLIN_LLM_TEST_FIXTURE` vs `KYLIN_LLM_PROVIDER` README 口径统一（非阻断）。

---

## ★最新权威状态（dev=`7762d63`，审阅窗口收口，2026-06-19 之二十五）

> 取代上方所有块为当前权威。**L backlog #2（README 口径统一）收口**：纯 docstring 改 `KYLIN_LLM_PROVIDER=fixture/real` 为实际开关，标注 `KYLIN_LLM_TEST_FIXTURE` 是 docstring 残留。

**链（origin 已 push 同步）**：`e2bd033`（L health 端点）→ **`7762d63`（L docstring 口径统一 merge）**

**交付内容**（L 域，纯 docstring 零代码）：
- `backend/app/llm/real_client.py` 模块顶部 docstring：`KYLIN_LLM_TEST_FIXTURE=true/false` → `KYLIN_LLM_PROVIDER=fixture/real`；加注脚"历史口径 `KYLIN_LLM_TEST_FIXTURE`（true/false）只是 docstring 残留，**不是实际开关**"。
- `scripts/demo_stage4_common.py` `build_e2e` docstring line 117：`KYLIN_LLM_TEST_FIXTURE=false` → `KYLIN_LLM_PROVIDER=real`。

**已核验**（审阅窗口亲跑，非采信）：
1. 行为零变化：`load_real_llm_config_from_env` / `_env_or` / `completion_fn` / `health_check` 全部未动；git diff --stat 确认仅 2 文件 +10/-5 ✓
2. 四道闸全绿：ruff / ruff-format / mypy / pytest 全 Passed（无 reformat 警告，证明纯 docstring 改动）✓
3. pytest 真值：**501/17** = 基线（纯 docstring 零回归）✓
4. C3 边界：2 文件均在 L 域，D/X 域零 diff ✓
5. L backlog 推进：**#2（README 口径统一）完成** ✓

**L 报告 vs 真值**：**本次零偏差**（§5 红线连续破例）。文件数 +10/-5 对；501/17 对；纯 docstring 对。

**L backlog 现状**：
- ✅ #2 README 口径统一（7762d63）—— 本次完成
- ⏳ #1 `?probe=true` 主动连通性探测（health 端点 backlog，非阻断，按需后续）
- ⏳ 阶段6（X P1 SSO/LDAP 阻塞中）

**遗留（不变）**：SSO/LDAP（X P1，阻塞阶段6）；SSE heartbeat（X P2）；`?probe=true`（L #1）。

---

## ★最新权威状态（dev=`7762d63`，审阅窗口收口，2026-06-19 之二十六）

> 取代上方所有块为当前权威。**另一窗口架构审阅（VM 真跑前查缺补漏）结果落地**：
> - **2 真 Blocker 落 X 域**（B1 SSE use-after-close / B2 whoami `res.data`）+ **X 工单已起**
> - **B3 真 LLM live 口径**用 **ADR-0003** 钉死 demo-only（live API 仍 fake 有意识设计，非 TODO）
> - **5 条中低优治债**记入"阶段6 backlog"

**ADR-0003 落盘**：`docs/adr/0003-real-llm-demo-only-scope.md`（本地，2026-06-19 L 拍板 Accepted）。
核心：**真 LLM 接入范围冻结为两条窄路径** ——
1. `scripts/demo_stage4_e2e.py --use-real-llm`
2. D 在 VM 上的真端点实证（ADR-0002 论据）

**`backend/app/api/app.py get_llm()` 保持 fake 不接真 LLM**（设计意图，非 TODO）—— 理由：真 LLM 接进 live API 超出本阶段安全边界审查；多租户/计费/合规审计超出范围。

### 审阅报告核验结论（§5 红线：审阅窗口亲跑 = 唯一真值）

报告作者声明"亲跑 pytest 482/17"——**审阅窗口亲跑 = 501/17**。差 +19 不是"虚高"，是报告作者亲跑数错了。**不能采信报告 pytest 数字**，对外口径以审阅窗口 501/17 为准。

### 报告核验真值表

| 报告声称 | 审阅窗口亲核 | 判定 |
|---|---|---|
| pytest 482/17 | 亲跑 **501/17** | ❌ 报告数字错 |
| B1 SSE use-after-close | 字节级属实（`proxy.py:78-100` `async with` 块内 `return StreamingResponse(resp.aiter_bytes(), ...)` + 时间线 `cc4b527 → ade1c24`） | ✅ 真 Blocker |
| B2 whoami `res.data` 多取一层 | 字节级属实（`request.ts:43` + `chat.ts:660-661` + `catch{}` 静默） | ✅ 真 Blocker |
| B3 `get_llm()` 仍 fake | 字节级属实（`app.py:91` + docstring 自承） | ✅ 已用 ADR-0003 钉死 |
| Medium: keep 死字段 | 工具 spec 有 keep，但模板路径在本机不存在未深核 | ⏸ 阶段6 backlog |
| Medium: RuntimeError 绕过审计 | 属实（`real_client.py:236/250` raise + `orchestrator.py:191` 只捕 `httpx.HTTPError`） | ✅ 阶段6 backlog |
| Medium: install.sh 无 offline wheel | 属实（`install.sh:52` 直接 `pip install`，无 `--find-links`） | ✅ 阶段6 backlog |
| Low: NOTICE mcp 漂移 | 属实（`NOTICE:11` "mcp 运行时依赖经 pip 安装"，但 mcp 已移除） | ✅ 阶段6 backlog |
| Low: 前端 vitest 不进 CI | 属实（package.json 无 test script，ci.yml 0 命中 npm test） | ✅ 已含在 X 工单内（B2 漏网根因） |

### 阶段6 backlog（中低优治债，记入备忘以防忘）

| # | 优先 | owner | 治债项 | 状态 |
|---|---|---|---|---|
| T1 | High | **X** | **B1 SSE use-after-close**（BackgroundTask 方案）+ VM 复证 | X 工单已起，本次交付 |
| T2 | High | **X** | **B2 whoami `res.data` 一行修** + 成功路径 vitest 用例 | X 工单已起，本次交付 |
| T3 | — | **X** | vitest config + `npm test` 接 CI（治 T2 漏网根因） | X 工单已起，本次交付 |
| T4 | Medium | D | `log.compress_rotate` keep 死字段对齐（spec vs 模板） | backlog，待起 |
| T5 | Medium | L | rate-limit/token-cap RuntimeError 补审计 + 坏响应纳入重试 | backlog，待起 |
| T6 | Medium | L | `install.sh` pydantic-core wheel offline wheelhouse（治 LoongArch 无网部署） | backlog，待起 |
| T7 | Low | 全员 | NOTICE 销 mcp 注释 + 7 条 SPDX 占位 | backlog，待起 |
| T8 | Low | D | 决策③ `_CHANGE_TOOLS` 加可执行守卫单测（CI 拦截手工集合漏加） | backlog，待起 |
| T9 | Low | D | CMD001-003 正则局限（rm -fr / 等绕过）policy_loader 注释升为对外 advisory | backlog，待起 |
| T10 | P1 | X | 反代 Basic Auth → 真 SSO/LDAP（阻塞阶段6 启动） | backlog |
| T11 | P2 | X | SSE heartbeat keepalive | backlog |
| T12 | — | L | `?probe=true` 主动连通性探测（health 端点 backlog） | backlog |
| T13 | — | L | `get_llm()` docstring 改"demo-only 设计意图，非 TODO"（ADR-0003 后续） | backlog |

### X 工单摘要（B1+B2+vitest）

- **修法**：B1 用 `from starlette.background import BackgroundTask` + `background=BackgroundTask(resp.aclose)`（non-SSE 顺手 `await resp.aclose()`）；B2 `chat.ts:660-661` 改 `res.user` / `res.roles`（去多取一层）
- **VM 复证**：B1 必须 VM 经反代真跑 SSE + R3 审批续跑（断流 = 修复失败）
- **前端测试**：新 `vitest.config.ts` + `package.json` `test` script + 成功路径用例 + ci.yml 加 `npm test` 步骤
- **拆 commit**：建议 2-3 commit（B1 SSE / B2 whoami / vitest 接 CI）

**遗留（不变）**：SSO/LDAP（X P1）；SSE heartbeat（X P2）；`?probe=true`（L #1）；其余 T4-T9 见上表。

---

## ★最新权威状态（dev=`200a63c`，审阅窗口收口，2026-06-19 之二十七）

> 取代上方所有块为当前权威。**X P1 SSO/LDAP + P2 SSE heartbeat 已合 dev**：B1/B2/前端测试 + P1 LDAP client + P2 heartbeat 5 commit 一次性 merge。

**链（origin 已 push 同步）**：`7762d63`（L docstring 口径统一）→ **`200a63c`（X B1+B2+vitest+P1+P2 merge）**

**X 本次交付**（5 commit 全部合入 dev）：
- `456c6c3` **B1 SSE use-after-close** —— `BackgroundTask(resp.aclose)` + non-SSE `await resp.aclose()`（真 Blocker，VM 复证 PASS）
- `8abf71a` **B2 whoami `res.data` 多取一层** —— `chat.ts:651-661` 改 `res.user/roles` + `as unknown as T` 强类型
- `f1380d5` **vitest 接 CI** —— `vitest.config.ts` + `package.json` `test` + 2 用例 + ci.yml `npm test` 步骤
- `e482b6b` **ci.yml 注释转 ASCII** —— 消除历史 GBK→UTF-8 双重乱码
- `25c66bf` **P1 LDAP** —— `deploy/sso/ldap_client.py` mock/真实双模式 + proxy.py 删 `USER_ROLE_MAP` + 7 测试用例
- `d7fb5b3` → `f44b0a5`（autosquash）**P2 SSE heartbeat** —— `_sse_heartbeat` + `KYLIN_SSE_HEARTBEAT_INTERVAL` + 与 B1 BackgroundTask 共存

**已核验**（审阅窗口亲跑 + 字节级 + 测试数真值）：
1. C3 边界：9 文件全在 X 域，L/D 域零 diff ✓
2. pytest 真值：**501/17** = 基线（X 域 7 个新测试 + 0 回归）✓
3. 四道闸全绿：ruff / ruff-format / mypy / pytest 全 Passed（autosquash 修正后）✓
4. S9 守住：`KYLIN_LDAP_BIND_PASSWORD` 仅 docstring 注释（无硬编码），`kylin123` 仅 mock 路径 ✓
5. 7 个测试用例字节级真数（不像上轮 22/22 虚高）✓
6. ADR-0004 已落盘（KYLIN_LDAP_MOCK 部署硬阻断）✓

**ADR-0004 落盘**：`docs/adr/0004-ldap-mock-deploy-hard-block.md`（本地，2026-06-19 L 拍板 Accepted）。核心：`KYLIN_LDAP_MOCK` 默认 false，生产部署必须 false（lifespan 启动检查 + install.sh 显式 Environment 待补，**阶段6 backlog**）。防止"生产误开 mock"导致认证全空。

### §5 红线本轮结论

- X P1 报告"7 用例全 PASS"数字真对 ✅（纠上轮 22/22 虚高）
- 1 处预测偏差：X 报告"proxy.py 无非 ASCII 字节 ruff-format 不会 reformat"——**实改 3 文件**(BOM 移除 + 旧式类型注解 `Dict`/`List` → 内置 `dict`/`list` + 空行规范化)，**逻辑零变化**，不留痕只记偏差
- 后端四道闸 + 7 个 X 测试 + pytest 501/17 全部亲核真值

**X backlog 现状**：
- ✅ P1 SSO/LDAP（200a63c，本次完成）
- ✅ P2 SSE heartbeat（200a63c，本次完成）
- ⏳ 阶段6.5 真 LDAP 实现（`raise NotImplementedError` 占位，等真 ldap3 server 就位）
- ⏳ ADR-0004 lifespan fail-fast 启动检查（防"生产误开 mock"）
- ⏳ ADR-0004 install.sh 显式 `Environment=KYLIN_LDAP_MOCK=false`

**遗留（不变）**：SSO/LDAP 实现（P1b 阶段6.5 阻塞）；SSE heartbeat（✅ 本次完成）；`?probe=true`（L #1）；其余 T4-T9 见阶段6 backlog。

---

## ★最新权威状态（dev=`05c58ee`，审阅窗口收口，2026-06-19 之二十八）

> 取代上方所有块为当前权威。**ADR-0004 部署硬阻断三道保险合入**（L 域）：systemd Environment 硬编码 + install.sh env 兜底 + app.py lifespan fail-fast。

**链（origin 已 push 同步）**：`d964935`（L ADR-0004 三 commit merge）→ **`05c58ee`（之二十八权威块 + 审阅交接 L 交接段归档）**

**交付内容**（L 域，4 文件 +113 行，3 commit 拆开）：
- `cd644d8` `deploy(systemd): kylin-safeops.service 硬编码 KYLIN_LDAP_MOCK=false` —— **第一道保险**
- `45d7d29` `deploy(install): 写 /etc/kylin-safeops/ldap.env 兜底` —— **第二道保险**
- `190d859` `fix(api): lifespan fail-fast 拒 proxy+mock=true + 3 monkeypatch 用例` —— **第三道保险**

**已核验**（审阅窗口亲跑 + 字节级，非采信）：
1. C3 边界：4 文件均在 L 域允许范围（`backend/app/api/app.py` + `backend/tests/` + `deploy/`），D/X 域零 diff ✓
2. pytest 真值：**504/17** = 基线 501 + 3 新增（亲核真值，**非采信**）✓
3. 3 个新 lifespan 用例 3/3 PASS（proxy+mock=true 拒 / proxy+real 过 / dev+mock 过）✓
4. 四道闸全绿：ruff / ruff-format / mypy / pytest Passed ✓
5. S9 守住：service + install.sh 不含 LDAP URL/账号/密码；env 兜底文件只含 `KYLIN_LDAP_MOCK=false` ✓
6. mode-aware 模式：fail-fast **仅 proxy 模式触发**，dev 模式放行（决策⑨ 同款）✓
7. 既有 `test_api_lifespan.py` 3 用例（fail_closed wiring）仍过，零回归 ✓
8. 8 文件（`.gitignore` + 4 根级 md + 3 ADR）已 push 入版本控制（审阅窗口全员决定 2026-06-19）✓

**L 报告 vs 真值**：**本次零偏差**（§5 红线连续 2 次破例）。4 文件 +113 行（11/81/17/4 数学吻合）✓；504/17 ✓；3/3 PASS ✓；C3/S9 ✓；fail-fast 字节级对。

**部署前硬阻断现状**（之十九 + ADR-0004 拍板后 = 全部清零）：
- 决策⑨ mode-aware 认证（proxy fail-closed / dev 联调放行）✅
- ADR-0001 SQLite 选型冻结 ✅
- ADR-0002 阶段5 真实 LLM 接入麒麟 V11 实机收口 ✅
- ADR-0003 真 LLM demo-only 范围冻结 ✅
- ADR-0004 KYLIN_LDAP_MOCK 部署硬阻断（本次）✅
- ⏳ **真 LDAP 实现**（P1b 阶段6.5，等 ldap3 server 就位）
- ⏳ **真 SSO/LDAP 替换反代占位 Basic Auth**（X 域 P1，阶段6 启动）
- ⏳ **SSE heartbeat**（✅ 本次 X 合入）

**L backlog 现状**：
- ✅ #2 README 口径统一（e2bd033 / 7762d63）
- ✅ ADR-0004 收口（本次）
- ⏳ #1 `?probe=true` 主动连通性探测（health 端点 backlog，非阻断）
- ⏳ 阶段6 阻塞中（等真 LDAP / ldap3 server 就位）

**遗留（不变）**：SSO/LDAP 真模式实现（P1b 阶段6.5）；`?probe=true`（L #1）；其余 T4-T9 见阶段6 backlog。

---

## ★最新权威状态（dev=`8848918`，审阅窗口收口，2026-06-19 之二十九）

> 取代上方所有块为当前权威。**P1b 真 LDAP 实现合入 dev**：ldap3==2.9.1 依赖 + Ldap...test(ldap): 13 用例（7 mock + 6 真模式 mock 全 ldap3 不触网）` — 5 安全红线（不区分用户不存在/密码错、防 injection 转义、size_limit=1、双超时 5s、异常吞掉）字节级全验 ✅
- **审查补修**：L 报告"基线 511+6=517"虚高（真值 504/17 = 498 + 6），ruff 3 errors（2 行长度 + 1 死代码）+ constraints.txt 重复行 bug + `test_real_size_limit_one_and_timeouts` 只断 1 个 kwarg（report "4 个 kwarg" 虚报）已审阅窗口补到位
- **C3**：4 文件均在 L 域允许范围，D/X 零 diff
- **S9**：ldap_client.py 无明文凭据，全走 env（`KYLIN_LDAP_BIND_PASSWORD` 等）
- **零回归**：pytest 504/17（基线 498 + 6 真模式新增，13/13 PASS）

**遗留**：真 LDAP server 端到端 VM 实证（需真 ldap3 server 上线，D 在 VM 跑）；X P1 真 SSO 替换反代占位 Basic Auth 留阶段6.5。

---

## ★最新权威状态（dev=`3b1f47a`，审阅窗口收口，2026-06-19 之三十）

> 取代上方所有块为当前权威。L #1 probe + T5-T9 + DN 归一化全部合入。

**链**：`18a032e` → `3b1f47a`（6 commit merge，10 文件 +332/-44，512/17 PASS）

**交付**（L 域，6 commit 拆开）：
- probe：`/api/llm/health?probe=true` + schema + RealLLMClient.probe() 独立 budget + 6 测试（S9 全守）
- T5：rate-limit/RuntimeError 走审计（orchestrator.py）
- T6：install.sh --find-links wheels/ 离线 wheel（LoongArch 无 Rust 编译）
- T7：NOTICE 销 mcp 漂移；T10：CMD001-003 补 `/*/.* rm` 变体正则
- T9：_CHANGE_TOOLS CI 守卫 +2 用例；**P1b DN 归一化**（_normalize_group_name，阻断D工单阻断2）
- T4/T8 正确跳过（T4 D 域，T8 X 已闭）

**审阅窗口补修**：L 数字虚高（519/17→真512，基线沿用错误517）；DN归一化漏实现（审阅窗口直接补）

**D 工单回执**：①slapd 确认 ②DN归一化已合入（裸名键对真LDAP成立）③intent 表见审阅交接之三十

---

## ★最新权威状态（dev=`d60e4a7`，审阅窗口收口，2026-07-11 之三十一）

> 取代上方所有块为当前权威。X P4 (SSE done 截胡修复 + Demo/Tools 风险等级卡片配色) 合入 + L 4 Router 工单落盘。

**链**：`73d8aa2` → `d60e4a7`（X 3 commit fast-forward）

**X P4 交付（已合）**：
- `657ed6a` feat(frontend): ToolsView 卡片背景色按风险等级映射（R0/R1/R2/R3 4档）
- `52cc9e6` feat(frontend): DemoView 卡片背景色与 ToolsView 一致（统一口径）
- `d60e4a7` fix(frontend): onDone 不再强关打字机 — SSE done 等待 tick 走完（1行删 chat.ts:706）

字节级核：3 文件全 frontend/，D/L 零 diff；四道闸全绿；pytest 512/17 零回归。

**L 域 backlog（4 Router 全补）**：

- ✅ 已合 dev=`9002e10`（之三十二，6 commit）：`/api/approvals` 5 / `/api/audit/*` 4 / `/api/policy/*` 3 / `/api/demo/*` 3 接口；pytest 530/17
- ✅ 已合 dev=`154f767`（之三十三，3 commit）：verified 后 LLM 自然语言总结 `natural_language` 事件 + 决策⑫ 间接注入防御纵深；pytest 534/17
- ✅ 已合 dev=`ed3ef82`（之三十四，1 commit + amend）：X A7 SSE Content-Type 声明 `charset=utf-8` 防中文 Latin-1 乱码；pytest 537/17（+3 charset 用例）
- 工单落盘：`.claude/l_4_router_workorder.md`
- 预计 5 commit（approvals / audit / policy / demo / 挂载+测试）
- 预计 pytest 512+15=527/17

**X 自检报告（4 页 API 接入现状）消化**：
- Chat SSE 主链路 ✅；其余 REST 全通；VITE_MOCK_ENABLED=false 真实模式下 4 页降级
- 4 Router 后端缺失是 L 域 backlog，**不阻塞主链路**

**SSE done 截胡修复方法确认**：`chat.ts:706` 删 `this.stopAssistantTyping(traceId)` — onDone 只清 SSE transport 状态(`loading=false`)，不写业务事件（打字机走完 tick 队列才停）。

---

## ★最新权威状态（dev=`c1e8c51`（未动），L 分支 feat/l-4-router-completion @ `990012e` 之三十二待审阅，2026-07-11）

> 取代上方所有块为当前权威。**L 域 4 Router 工单已完...80885/17。

---

## ★最新权威状态（dev=`9002e10`（未动），L 分支 feat/l-verified-natural-language @ `c4c86dc` 之三十三待审阅，2026-07-12）

> 取代上方所有块为当前权威。**L 域 verified 后 LLM 自然语言总结工单完成**（dev 未动，分支未推）。

**工单背景**：前端聊天区"对工具结果自然语言总结"在 X 域 P4 SSE done 修复后被 X 提上日程；X 不动，前端依赖 L 域后端补 natural_language 事件。

**实施**（feat/l-verified-natural-language 分支 3 commit）：

| commit | 类型 | 内容 | 文件数 |
|---|---|---|---|
| `db75ca0` | feat(stream+orch) | EventType += "natural_language" + 决策⑫ 间接注入防御纵深 `detect_tool_output_injection` + orchestrator verified 后调 `_emit_natural_language` | 3 (+114/-1) |
| `c4c86dc` | feat(llm+test) | LLMAdapter.summarize + fake `_fake_summary_fn` async + RealLLMClient.summarize + S9 浅过滤 `_sanitize_for_summary` + 4 用例 | 5 (+367/-7)（含 commit 1 E501 收口） |
| `HEAD`（pending） | docs+test | 4 文档 之三十三 留痕 + S9 守门 test（T4 spy 验证 api_key/bind_password REDACTED）| 5（含 4 文档） |

**契约变更**：
- `backend/app/contracts/stream.py` EventType Literal += `"natural_language"`；
  data `{text: str, sensitive_filtered: bool}`，sensitive_filtered=True 表示 S9 已 REDACTED。
- S3 不破：natural_language **不进 audit 哈希链**（仅 SSE 流式推送，前端聊天区）。
- 决策⑫ 扩展接口已实现：`detect_tool_output_injection` 复用 `_PATTERNS` 仅看 high severity；
  拦下 → 仅 audit 一行 `natural_language_gate=deny`（不可信，不进 SSE），emit 跳过。

**间接注入防御纵深**（决策⑫）：
1. `_execute_batch` emit "verified" 后 → 调 `_emit_natural_language(results)`
2. 三段：a) 拼接 tool_results 文本 → `detect_tool_output_injection` 拦 high 风险；b) LLM.summarize（fake 固定 / 真 LLM 走 httpx）；c) emit natural_language
3. LLM 调失败（超时/异常）→ log warn **不阻断 FINISHED**（S8 fail-closed 不杀状态机，前端仍可见 verified/tool_result 推断结论）

**S9 守门**（RealLLMClient._sanitize_for_summary 浅过滤）：
- 6 类黑名单：api_key / authorization / bind_password / secret / token / password
- 浅 dict 替换值 → `***REDACTED***`；key 名原样保留
- T4 spy 测试验证 LLM 喂料不含明文（已在 commit 3 落地）

**测试**：
- pytest **534/17** = 530 基线 + 4 新增（T1 fake 固定字符串 / T2 timeout 不阻断 / T3 间接注入拦下 / T4 S9 REDACTED）
- 四道闸 ruff / ruff-format / mypy / pytest 全 Passed
- C3 边界：全 backend/app + backend/tests（无 frontend / mcp_servers / deploy/proxy 改动）

**冻结契约不动**：tool/intent/policy/untrusted/audit/stream schema 字节级；ADR-0001/0002/0003/0004/决策①-⑬ 全部不翻案；S1-S9 安全不变量全守。

**已知风险**：
- fake LLM summarize 返固定文本（"已完成:<tool_names>"），与真 LLM 效果存在差距；

---

## ★架构审计 + 整改工单核验后真实未合 L backlog（dev ac4ba0e 之三十八，2026-07-13）

> **核验过程**：架构者读 `架构审计报告-dev-154f767.md` + `整改工单-154f767-L.md`，
> 字节级对照当前 dev=`ac4ba0e` 代码逐条核**真伪**（审计报告与整改工单**有部分不准**），
> 下面是真未合 L 域 backlog 18 项（按批分）。

### B1 Blocker（审计 durability，重启/掉电即断链）— 3 commit
- **L-B4-1** `orchestrator._audit.append()` 同步阻塞未 `asyncio.to_thread`（整改 B4-L 真）
- **L-B4-2** lifespan shutdown 不 drain 后台 orchestrator task（整改 B4-L 真，审计报告 B4 漏）
- **L-B4-3** `SqliteAuditSink` 未接 `synchronous=FULL` + `busy_timeout`（整改 B4-L 真）

### B2 授权层（广谱 IDOR + 审计全史开放）— 4 commit
- **L-H1** `backend/app/api/routers/sessions.py` create/update/delete **无 owner 校验**（审计 H1 真）
- **L-H2** `/api/audit/*` 4 端点**只 require_proxy_identity 缺 `require_role({"auditor","admin"})`**（整改 H2 真）
- **L-M3** 审计链无 actor / SoD（整改 M3 真）
- **L-M4** `/api/policy/*` 缺 auditor 限（整改 M4 真）

### B3 LLM 硬化（summarize prompt 铁律1/4 缺）— 2 commit
- **L-H9** `RealLLMClient.summarize()` **prompt 未套 `wrap_many_for_feedback` + `_GUARD` 守卫**（整改 H9 真 + 我亲读核：当前 prompt 是裸 `tool=X\nexit_code=N\nstdout=...`，无 BEGIN/END 定界、无"以下是工具执行输出..."守卫句，攻击者可借 stdout 注入"忽略以上指令"）
- **L-H5** HMAC canonical 仅 `user\nroles\nts`（审计 H5 真），**未绑 method/path/body_sha/nonce，无 ±300s LRU 缓存防重放**（整改 H5 扩展项真）

### B4 韧性 + 可观测 — 2 commit
- **L-H14** `EventBus.create()` 用 `asyncio.Queue()` **无 maxsize**，无 QueueFull 处理，无 registry.active_count 上限（整改 H14 真）
- **L-H16** logging.dictConfig(JSON formatter + trace_id context) **全无**，无 /health readiness 端点（整改 H16 真；当前 `/api/llm/health` 是 LLM 配置态而非 readiness）

### B5 CI + 灰度 Medium — 4 commit
- **L-H12** pyproject.toml testpaths **不含 deploy/sso deploy/proxy**，mypy files regex **不含 deploy/(sso|proxy)/**（整改 H12 真）
- **L-M5** 请求体大小无上限，无 Content-Length 中间件（整改 M5 真）
- **L-M6** HTTPException detail 暴露内部信息（整改 M6 真）
- **L-C1** `/api/llm/health/events` SSE 端点缺 `require_proxy_identity`（任何人可订阅 probe 失败历史）— 你之前列

### B6 注入共谋 + 降级 + 修真 — 3 commit
- **L-C2** `audit_export` Authorization header 浅过滤未做（S9 加固）— 你之前列
- **L-C5** D-10 注入共谋场景未覆盖（直接注入 user_intent + 间接注入 tool_result stdout 同时触发）— 你之前列
- **L-C6** `natural_language` 失败时无降级回显（前端不应只看到 verified.tool_result 原文）— 你之前列
- **L-M1** escalate `except:pass` 已删，但 seq 修真需核（整改 M1 真）

### 报告/整改 工单不准确项（我亲读发现的坑）
- **审计报告 B4** 漏列 drain 后台 orchestrator task（整改 B4-L 列出，**以整改工单为准**）
- **整改 H9** "summarize 套 wrap_many_for_feedback"描述抽象，**真问题：summarize prompt 缺铁律1/4 定界**（应 `wrap_many_for_feedback(...)` + `_GUARD` 守卫，**整个 prompt 序列化逻辑重构**，不是简单套函数）
- **整改 H5** 整改工单比审计报告**多 3 项**：nonce + LRU 缓存（防重放），**以整改工单为准**

### §5 红线偏差留痕
- 架构者 **核基线失误 2 次**（之前凭记忆列 L-D1/T13/T5/T6 当新工单，全部已合；列 X-proxy SSE 阻塞已合；列 D P0/P1/P2 全已合）
- **本轮改进**：每条候选必须 `grep / git log --grep / sed -n` 字节级核真伪，再列 backlog
  真端点 httpx POST 待 D 在 VM 配 env+密钥实跑（`KYLIN_LLM_SUMMARIZE_TIMEOUT=5s`）。
- `_sanitize_for_summary` 浅过滤仅看顶层 key；嵌套 args.api_key 不会被过滤（实测覆盖 by 现有工具结果
  schema 顶层已含 stdout/exit_code；嵌套 args 里的 api_key 在 tool_args 闸/结果闸阶段已被拦在 LLM 之前）。

---

## ★最新权威状态（dev=`a82f984`（未动），L 分支 feat/l-x-integration-5-apis @ `b84b55d` 之三十五待审阅，2026-07-13）

> 取代上方所有块为当前权威。**L 域 X 接入联调 5 接口缺失 + 1 §5 守门（合批 5 commit）工单完成**（dev 未动，分支未推）。

**工单背景**：X 完成前端 natural_language SSE 接入 + 4 Router 真实接口切 + DemoView 场景对齐（已合 dev=`a82f984`，含 A7 charset=utf-8）。联调中 X 报 6 问题，归 L 域 5 接口 + 1 §5 红线守门。**D2 §5 红线先打回 1 条**：`get_llm()` 硬编码 `build_fake_llm()` 对 Chat 不生效 → 不是 bug，是 ADR-0003 demo-only 设计意图；本工单不修改 get_llm() 但**新增守门测试**证明 Chat 永远走 fixture（防误改）。

**实施**（feat/l-x-integration-5-apis 分支 5 commit）：

| commit | 类型 | 内容 | 文件数 |
|---|---|---|---|
| `718074f` | fix(rca) | RCAAnalyzeRequest evidence + 响应 evidence_count | 3 (+137/-5) |
| `82c71d7` | feat(overview) | services 真填 + /overview/history + /stats | 3 (+468/-11) |
| `fd3807b` | feat(tools) | /api/tools/calls/{call_id} 详情 + S9 REDACTED | 3 (+256/-2) |
| `b84b55d` | test(api) | D2 §5 红线守门 — Chat 永远走 fixture | 1 (+141/-0) |
| `HEAD`（待 commit） | docs | 4 文档 之三十五 留痕 | 5（含 4 文档） |

**5 接口补齐**（X 联调）：

| 接口 | 类型 | 修复内容 |
|---|---|---|
| `/api/rca/analyze` | R3 | 接 evidence list[dict] + 响应 evidence_count > 0 表示真接 |
| `/api/system/overview` | D3 | services / tool_calls_today / denied_today 从审计库真填 |
| `/api/system/overview/history` | D1 | 新增 hours=1..168 sparkline（当前 series 空，pending overview_probe 落库） |
| `/api/system/stats` | D5 | 新增 by_tool / by_risk / by_status 三维度聚合 |
| `/api/tools/calls/{call_id}` | D6 | 新增详情端点（MVP call_id = trace_id，末条 EXECUTING/EXECUTED 派生） |

**§5 红线守门（D2）**：新增 3 用例证明 Chat 路由永远走 fixture（ADR-0003 demo-only 锁死）：
- T9: `KYLIN_LLM_PROVIDER=real` + spy `RealLLMClient.__init__` → POST /api/chat 后 spy 计数 == 0
- T10: 直接调 `app.get_llm()` → 断言返回对象模块名不是 `real_client`
- T10b: spy `RealLLMClient.completion_fn` → `get_llm()→plan()` 后 spy 计数 == 0

**测试**：
- pytest **553/17** = 537 基线 + 3 RCA + 6 overview + 4 tools + 3 D2 守门 = 553 数学吻合 ✅
- 四道闸 ruff / ruff-format / mypy / pytest 全 Passed
- C3 边界：12 改动文件全 backend/app + backend/tests + 4 项目根 *.md（无 frontend / mcp_servers / deploy/proxy）

**关键设计决策**：

| 决策 | 落地 |
|---|---|
| ADR-0003 demo-only 范围 | Chat 不接真 LLM；守门测试钉死，未来误改立刻红 |
| 决策⑨⑬⑭ 不动 | mode-aware / RBAC fail-closed / 契约原名 全守 |
| S9 守门 | `get_tool_call_detail` payload.args 走 `_SENSITIVE_KEYS` 黑名单（与 audit_logger 一致）→ ***REDACTED***；T7-S9 验明文不漏 |
| 审计 SQL 直查 | `_audit_conn(audit)` helper 拿 `_conn`（与 `routers/policy.py` 同模式）；非 SqliteAuditSink 实现返 None 兜底 |
| hours clamp | `hours: int = Query(1..168)` 防单次查全库 |
| overview 探针 | 当前未存 `overview_probe` phase → history series 空；端点接口就位待探针落库（不改方案 b） |

**冻结契约不动**：tool/intent/policy/untrusted/audit/stream schema 字节级；ADR-0001/0002/0003/0004/决策①-⑬ 全部不翻案；S1-S9 安全不变量全守。

**已知风险**：
- `/api/system/overview/history` 当前 series 空（审计未存 overview_probe phase；保持方案 b 豁免审计）；前端 sparkline 显示"暂无数据"
- audit 库 phase 索引仅 `(trace_id, seq)`，按 phase 全表扫在 10k+ records 时可能慢；stats 端点加 LIMIT 10000 兜底
- D2 守门测试依赖 `RealLLMClient.__init__` spy + 模块名断言；未来若重构 get_llm() 内部（如抽 factory 函数），断言需同步更新

---

## ★最新权威状态（dev=`c99d760`（未动），L 分支 feat/l-probe-audit @ `721d452` 之三十六待审阅，2026-07-13）

> 取代上方所有块为当前权威。**L 域 ?probe=true 审计化 + SSE audit_appended 工单完成**（dev 未动，分支未推）。

**工单背景**：X 联调指出 `/api/llm/health?probe=true` 失败时审计不写、前端看不到 probe 失败历史；上游 prompt 设计失误（4 条候选 backlog 已全合 dev 后）补加本 P1 工单。

**实施**（feat/l-probe-audit 分支 2 commit）：
- `da8072a` feat(llm+api): `RealLLMClient.probe(audit_sink=...)` + `/api/llm/health?probe=true` 失败时落审计（phase=probe_failed, trace_id=probe-{epoch_ms}）+ emit SSE audit_appended 到固定 channel "probe-watch"；新增 `/api/llm/health/events` SSE 端点供前端订阅 probe 失败历史。
- `721d452` test(llm): probe 审计化 4 用例（T1 失败审计 / T2 SSE audit_appended / T3 timeout 审计 / T4 fixture 不写审计）。

**判定**：4 个新用例 + 0 回归；pytest **557/17**（基线 553 + 4 数学吻合）；四道闸全绿；S9 守住（payload 仅 model/base_url 不含 api_key 明文）；签名 LEBLINC<L 邮箱>（无 AI 署名）。

**已知风险**：
- probe 审计 trace_id 固定格式 `probe-{epoch_ms}`；同毫秒并发 probe 会撞 (trace_id, seq) UNIQUE → 当前未防御；
  backlog 后续可加随机后缀或加 timestamp 微秒精度。
- probe SSE channel "probe-watch" 是单 channel；多客户端订阅会瓜分事件（同 _event_queue 互斥消费语义）；
  与 chat trace SSE 同口径，待 L 决策是否走多 channel。
- probe audit trace 单 phase 不归入 orchestrator 哈希链（probe 是运维一次性触发，非 agent 请求流）；
  走独立 GENESIS 起点；前端查 `/api/audit/traces/{probe-ts}` 时 verify_chain 应返 valid=True（单条记录即链）

---

## ★最新权威状态（dev=`febd7e5`，L 域后续工单待架构者分派，2026-07-13 之三十七）

> 取代上方所有块为当前权威。**L 域下一次工单待架构者选定**——已知 backlog 见审阅交接 之三十七。

**架构者视角盲区记录**：
- 之三十六 合 dev=... probe 失败走 audit + SSE audit_appended
- 之三十五 合 dev=a82f984 L 域 X 接入联调 5 接口 + D2 §5 守门
- 之三十四 合 dev=ed3ef82 X A7 SSE charset=utf-8

**当前 dev 链**（fe...base=febd7e5 / origin=febd7e5，... 等。

---

## ★最新权威状态（dev=`f9f17a6`（未动），L 分支 feat/l-b1-audit-durability @ `a1912ee` 之三十九待审阅，2026-07-13）

> 取代上方所有块为当前权威。**L 域审计库 durability Blocker 工单完成**（dev 未动，分支未推）。

**实施**（feat/l-b1-audit-durability 分支 2 commit）：
- **`5925a77` L-B4-3**：`SqliteAuditSink.__init__` 加 `PRAGMA synchronous=FULL`（断电安全）+ `busy_timeout=5000`（写并发抗锁死）；新增 `flush()` 方法（PRAGMA wal_checkpoint FULL）+ `close()` 顺序改为 flush → close。S8 兜底（PRAGMA 失败 / flush 失败仅 logger.warning，不 raise 不杀状态机）。S3 字节级不动（hash 链仍走 contracts.audit.compute_curr_hash）。
- **`a1912ee` L-B4-2**：`app.py` lifespan shutdown 段改顺序 drain（registry → bus → audit），`registry.snapshot()` 拉所有 RUNNING orchestrator task → `asyncio.wait_for(shield, timeout=2.0)` → 超时 task.cancel()；`EventBus.drain_all()` 清空 queue；`audit.flush() + audit.close()`。S8 兜底（任何阶段异常不影响后续步骤）。
- **L-B4-1 to_thread 跳过**：Windows sqlite3 thread safety 问题（`asyncio.to_thread` + SqliteAuditSink + check_same_thread=False 组合在 Windows 上抛 access violation）。留 P2 backlog，需进一步调研（候选：①每个 sink 实例独立 thread、②asyncio.Queue + 后台 worker、③放弃 synchronous=FULL 改 NORMAL）。

**已知风险**：
- `synchronous=FULL` 在 Windows fsync 较慢（实测 5-10ms per write）；生产高并发场景需评估 QPS 影响
- `busy_timeout=5000` 高并发下 SQLite contention 等待时间可能上升
- L-B4-1 未实施：审计同步 write 仍阻塞 event loop（高频审计场景延迟敏感）
- `registry.snapshot()` 暴露内部 `_sessions` dict（封装不够；后续可加 `active_sessions()` public API）


---

## ★最新权威状态（dev=`122cc62`（未动），L 分支 feat/l-b2-auth-layer @ `fa1184d` 之四十待审阅，2026-07-13）

> 取代上方所有块为当前权威。**L 域授权层 Blocker 工单部分完成**（dev 未动，分支未推）。

**已落**：
- commit 1 `d6402a1`（L-H1 IDOR 修复）：sessions 5 端点 + chat session_id owner 校验
- commit 2 `fa1184d`（L-H2/L-M4 require_role helper）：rbac.py 新增 require_role + roles_satisfy，**router 接线回滚**（防回归现有 25+ rbac/policy/audit 测试）

**未落**（留 P2 backlog）：
- commit 2 router 接线：audit.py + policy.py 6 端点 require_role 替换 require_proxy_identity（dev 模式测试需带 X-User-Role 头；本轮没...$
- commit 3 L-M3 审计链记 actor + SoD（orchestrator principal 注入 + 审计 payload + 拒批 SoD 校验）：复杂改造，本轮 skip
- commit 4 IDOR 守门测试 10 个（3 sessions + 4 audit_role + 3 audit_actor_sod）：本轮 skip（依赖 commit 1-3）

---

## ★最新权威状态（dev=`9...授权层 B2 + ADR-0005 起 + ADR-0005 留底）。**X D2** KYLIN_LLM_RECORD 录制已并工单可合 dev。**P1 + D7** 留 B2' 完后另起新工单（独立 1-2 commit）。

---

## ★最新权威状态（dev=`974505c`（未动），L 分支 feat/l-b2-deviations @ `03e1af9` 之四十一待审阅，2026-07-13）

**B2 5 偏差 4 项实际落地（commit 1/2/3/4）+ 偏差5之四十 commit 3d96fc9**：dev 未动，分支待审。
...- L-B2-5 conftest 验证 L-B2-4 f3a1 状态（KYLIN_AUTH_MODE=proxy + signed），唐通 context。
- 简记 高/志：进守门良是 组合全守（test_b2 10 守门 + proxy 严测 2 用例）。


---

## ★最新权威状态（dev=`974505c`（未动），L 分支 feat/l-b2-deviations @ `03e1af9` 之四十一待审阅，2026-07-13）

> 取代上方所有块为当前权威。L 域 B2 偏差 5 项（4 实际 + 1 已 PASS）补完，dev 未动，分支待审。

**5 commit 拆开（单主题守）**：
- `f4cdeb3` fix(approvals) SoD check：actor == approver + approve=True → audit sod_violation + 403；admin 旁路 `_admin_bypass_sod` 用于运维撤审
- `6ac4208` test(auth) B2 10 守门测试：3 sessions_owner（T1-T3）+ 4 audit_role（T4-T7）+ 3 audit_actor_sod（T8-T10）
- `a22f7d6` fix(deps) KYLIN_TEST_X_USER_ROLE 由死配置晋升为 principal_for_idor fallback（header > env 优先级）
- `03e1af9` test(auth) proxy_mode_client + proxy_signed_headers fixture + 2 audit proxy 用例（admin→200 / viewer→403）
- `3d341d4` test(policy) 评审窗口 X-User-Role header cherry-pick（B2 takeover 留痕）

**测试真值**：pytest **578/17** 实跑（基线 566 + 12 增量：10 test_b2 + 2 audit proxy）。

---

## ★最新权威状态（dev=`1daf3d4`，2026-07-14 补留底 之四十二-之五十一）

> 修正 §5 红线缺口：B2 之四十一之后 7 个空号（之四十二-之四十八）+ 后续工单 4 文档留底补完。
> 之四十一后续所有 merge 一次性补留底，dev=`1daf3d4` 权威。

### 链路

| 编号 | 内容 | 状态 |
|---|---|---|
| **之四十二** | B3 LLM Adapter safety + 真接 LLM 守门（commit 919ca42 L-H9 summarize guard / 97482d8 completion_fn 异常 / 6bdf9bf schema retry decision⑫）| ✅ 已合 dev |
| **之四十三** | ADR-0005 demo-record-mode（commit 56791c0 get_llm 分支 / 67d1122 D2 §5 守门修订 T11 / b2eedde ADR-0005 文档）| ✅ 已合 dev |
| **之四十四** | X 4 Router 真接口联调（commit 3a11339 P0/P1/P2 / fdaf54f 假数据移除 + 弹窗）| ✅ 已合 dev |
| **之四十五** | B4 EventBus maxsize + logging dictConfig + /health/ready（commit 26fb5c3 / 1a54173 / 8193923）| ✅ 已合 dev |
| **之四十六** | B4 P2 SSE QueueFull 兜底（commit 4cbfa82 chat._event_source try/except EventBusQueueFull + yield SSE error event）| ✅ 已合 dev |
| **之四十七** | D bug probe-watch SSE 守门（commit a29df1b bus._queues 守门 + long-life channel finally 验证；arch 接管修 ruff F841）| ✅ 已合 dev |
| **之四十八** | B6 + B5 P3 收口（commit 0f20b37 注入共谋 / 117de7a summarize fallback / a636974 escalate 修真 / 2d61c8e chunked body ASGI / 1ba4cf9 handler 守门）| ✅ 已合 dev |
| **之四十九** | 阶段5 step 2 真接 LLM 收口 + ADR-0006 real-mode-by-default 生效（commit 5.1-5.5）| ✅ 已合 dev |
| **之五十** | X byte-verify REJECTED 路径（commit 6f258a2 T1+T2 双 PASSED，后端 CLEAN，真根因=前端 fallback）| ✅ 已合 dev |

### 偏差留底（补留底中暴露的 §5 红线缺口）

- **L 报告"四道闸全绿"虚高 5 度**（B5 P3 commit / D bug fix / B6 / 阶段5 step 2 / X byte-verify）—— **arch 接管 amend ruff check / format check**
- **L 工作树 `_t1_debug.py` 根目录脏文件** —— 架构者留底：根目录 `_*.py` 违反 C3（已删）
- **架构者接管预审失职**:B2 之四十一后 7 工单合并时**漏 4 文档 append** —— 本 commit 一次性补完

### 之五十一（前置留底：项目正式完成路线图）

| 工单 | 内容 | 状态 |
|---|---|---|
| 阶段5 step 2 + ADR-0006 | dev `5091a90` 合入：`get_llm()` 默认真接（KYLIN_LLM_FAKE=true opt-in 回 fake）| ✅ |
| X byte-verify | dev `1daf3d4` 合入：T1+T2 后端 CLEAN 守门 | ✅ |
| A1 SSO 反代替换 | dev `1daf3d4` 后续工单（KYLIN_PROXY_AUTH_SECRET 部署文档 + HMAC 加固 + nonce LRU）| ⏳ 执行中 |
| X 前端 REJECTED fallback | frontend/src/stores/chat.ts 修（X 域）| ⏳ X 在做 |
| L 增量守门 commit 1.5 | X 修完后 byte-verify | ⏳ 等 X 完 |
| A2/A3/B1/C2/... | "生产级别阶段6"12 大任务路线图 | ⏳ A1 完后起 |

---


---

## ★最新权威状态（dev=`d4ec90d`，2026-07-14 路线图 留底 之五十三）

### 阶段6 + 阶段7 路线图摘要

| 阶段 | 含义 | commit 数 | 时间预估 |
|---|---|---|---|
| 5 P4 | 5.3/5.4 留 P4 收口（rate_limit/token_cap phase 区分 + 决策⑫ e2e mock） | 2 commit | 1 周 |
| 6 启动 | A1/A2/B1/B2/C2/C3/D1/D2/J2（部署硬阻塞 13 项） | ~20 commit | 1 个月 |
| 6 重要 | E/F/G/I/J3/J4/K（运营级别） | ~20 commit | 2 个月 |
| 7 高级 | A4/B3/D3/D5/E4/G3-G5/H/I4/J5/K3/L（企业级 / 合规级） | ~40 commit | 4-6 个月 |

### 5.3 P4 + 5.4 P4 工单（架构者派）

- 5.3 P4 工单：rate-limit / token-cap audit phase 区分 + natural_language synthetic（仅 token_cap_exceeded emit synthetic=true）
- 5.4 P4 工单：间接注入 decision⑫ end-to-end mock real LLM
- 分支：feat/l-stage5-p4-rate-indirect（2 commit）
- pytest 预期：639/17 = 632 + 7 增量
- 4 文档 之五十三 留底（+1 递增）

---

---

## ★最新权威状态（dev=`d4ec90d`，2026-07-14 路线图 留底 之五十三）

### 阶段6 + 阶段7 路线图摘要（架构者补留底）

| 阶段 | 含义 | commit 数 | 时间预估 |
|---|---|---|---|
| 5 P4 | 5.3/5.4 留 P4 收口（rate_limit/token_cap phase 区分 + 间接注入 end-to-end mock） | 2 commit | 1 周 |
| 6 启动 | A1 SSO 反代替换 + A2/A3 + B1/B2 + C2/C3 + D1/D2 + J2 + 5.3/5.4 P4 收口 | 18 commit | 1 个月 |
| 6 重要 | E/F/G/I/J3/J4/K | 20 commit | 2 个月 |
| 7 高级 | A4/B3/D3/D5/E4/G3-G5/H/I4/J5/K3/L | 40 commit | 4-6 个月 |

### 在跑工单（已派执行窗口）

- **A1 SSO 反代替换**（HMAC 加固 + nonce LRU + 真接 LDAP 部署文档）— 4 commit，预留号 之五十二
- **5.3/5.4 P4 收口**（rate_limit/token_cap phase 区分 + 间接注入 end-to-end mock）— 2 commit，预留号 之五十三
- **X 前端 REJECTED fallback**（frontend chat store 修：state 校验 + 打字机不渲染）— X 域在跑
- **L 增量守门 commit 1.5**（X 修完后 byte-verify）— 待 X 完后起

---

---

## ★最新权威状态（dev=`8944a62`，2026-07-14 A1 SSO 反代替换 之五十二）

### A1 收口

| commit | 内容 |
|---|---|
| aa7048d | A1.1 HMAC canonical v2 (method/path/body_sha/nonce 防中途篡改+nonce 防重放) |
| 4897af6 | A1.2 nonce LRU 4096 + 300s 时窗 (防重放) |
| 6095899 | A1.3 真接 LDAP v2 链路 (proxy.py::sign v2 + body_sha middleware + 4 X-Auth-* 头) |

### P4 留底（架构者承认）

1. Redis nonce store（multi-replica 部署 in-process dict 不够）
2. 真 LdapClient real 模式 + httpx body_sha middleware 注入
3. 真接 LDAP 端到端 smoke test（需 LDAP 容器）

### §5 红线缺口第 11 度

- ruff-format Failed 1 file (test_nonce_lru.py) — 架构者接管 amend 修
- mypy Failed 1 file (deploy/sso/ldap_client.py types-ldap3 stub missing) — pre-commit 不查,CI 不阻

---

## ★最新权威状态（dev=`2207366`，2026-07-14 补留底 之五十三/五十四/五十五 完成实录）

> 修正：之前工单书面预留号"之四十五/四十六/四十七"与本文档已存在的 之四十五-之五十三 内容冲突（属过期信息）。
> 之五十三/之五十四 此前仅有"执行中/⏳"占位、无完成实录；D+X 修正合 dev 后从未留痕。本次一次性补完实录，
> 新工作留痕号顺延为 之五十五起。

- **之五十三** = 阶段5 P4 收口（分支 `feat/l-stage5-p4-rate-indirect`，已 merge dev）
  - `90fdce2` 5.3 P4：rate_limited/token_cap_exceeded phase 区分 + synthetic event（orchestrator.py +41/-；pytest 641/17）
  - `61fb552` 5.4 P4：间接注入 decision⑫ end-to-end mock 守门 4 用例 T12-T15（+165；pytest 645/17）
- **之五十四** = L 增量守门 commit 1.5（byte-verify X REJECTED 修复）
  - `0a0f69c` T16 user_intent 一致性 + 4 文档留痕守卫脚本 `scripts/check_docs_numbering.py`（+178；pytest 648/17）
- **之五十五** = D 阻断修复 + X P5 + X P6（此前未留痕，D+X 域联调收口）
  - `d3a4da7` D 阻断：deps.py v2 proxy verify 同步（method/path/body_sha/nonce 4 header）+ BodyShaMiddleware（+164；pytest 652/17）
  - `bfcb4e6` X P5：get_llm() 注入 summary_fn=real.summarize（+39；pytest 653/17）
  - `e8594a7` X P6：orchestrator._emit_rca_summary 决策⑫ 防御纵深 + adapter.py evidence kwarg 向后兼容（+66；pytest 656/17）
  - merge dev = `754e23b` → `2207366`（amend 修 ruff format）

pytest 实跑核验（本次亲跑）：**656 passed, 17 skipped**（与末 commit `e8594a7` 自报一致 ✅）。

---

## ★最新权威状态（分支 feat/l-a1-p4-redis-nonce，2026-07-14 之五十六 返修补留痕）

> 返修说明：本条为审阅裁决"阻断1"的补留痕——原 c2fa97d 只动 6 个 backend 文件，
> 4 文档零 append，之五十六缺失。现补齐完成实录。

- **之五十六** = A1 P4 redis nonce store（分支 `feat/l-a1-p4-redis-nonce`）
  - `4abc627` A1 P4.1：抽象 `NonceStore` Protocol + `InMemoryNonceStore`（原 `_SEEN_NONCES`
    dict + `_gc_nonces` 函数迁移封装）；`verify_proxy_identity` 改用 `_get_nonce_store()`；
    `KYLIN_NONCE_STORE=memory` 默认；T1 同 nonce 二次 seen→重放拦截 / T2 gc 过期清理
    （+122/-19；pytest 658/17）
  - `c2fa97d` A1 P4.2：`RedisNonceStore`（SETEX 写入 + EXISTS 查询，TTL=300s 与
    `_MAX_CLOCK_SKEW_SECONDS` 对齐）；`_build_redis_nonce_store()` 连接/依赖失败
    fail-soft 回退 `InMemoryNonceStore`（与 `ldap_client.py::_import_ldap3` 同一模式）；
    T3 mock redis 重放拦截 / T4 record 调 SETEX 一次 / T5 redis 不可用回退
    （+110；pytest 661/17）

pytest 亲跑复核（本分支独立基线）：**661 passed, 17 skipped**（658 + 3 = 661 数学吻合）。
## ★最新权威状态（分支 `feat/l-a2-wsproxy-startbat`，2026-07-14 之五十七 A2 wsproxy+start.bat 收口）

- **之五十七** = A2 wsproxy + start.bat 部署硬阻塞（分支 `feat/l-a2-wsproxy-startbat`，紧接工单2 之五十六）
  - `8a71d16` A2.1：wsproxy.py 真接 LDAP（`authenticate_and_build_headers` 纯函数）+ 抽 `_sign.py` 公共签名 helper（proxy.py 改 import 去重复）+ `deploy/proxy/__init__.py`（修复 tests 模块名冲突）+ T6/T7 守门（pytest 658/17）
  - `b2db94a` A2.2：`start.bat` Windows 启动脚本 + `REPLY_DEPLOYMENT.md` 补 start.bat 指引/wsproxy vs proxy 区分/流程图 + `env.example` 补 KYLIN_NONCE_STORE + T8 守门（pytest 659/17）
  - 返修 commit（本次）：`REPLY_DEPLOYMENT.md` §2 表格字段名纠正（`KYLIN_LDAP_BIND_PW`→`KYLIN_LDAP_BIND_PASSWORD`，`KYLIN_LDAP_USER_BASE`→`KYLIN_LDAP_BASE_DN`，补 `KYLIN_LDAP_GROUP_ATTR`），对齐 `ldap_client.py::_REQUIRED_REAL_ENV`

pytest 亲跑复核（本分支独立基线，不含工单2 的 redis nonce +5 增量）：**659 passed, 17 skipped**。

---

## ★完成状态校准快照（dev=`de0d5b6`，2026-07-15 刷新；非工单留痕·不占号）

> 目的：架构窗口亲核 dev 全部相关代码，校正本备忘中大量已过期的"⏳/预留/backlog"标记
> （多为架构审计报告基线 154f767 早期状态，若干 Blocker/High 已在后续 commit 闭合）。
> 本块为**状态索引校准**，非新工作交付，故不占 之X 留痕号；工单留痕从当前 4 文档真 max（之五十九）+1 顺延。
> 2026-07-15 刷新：原快照建于 dev=2087725，此后 之五十八（D 域 v2 收尾 bug）+ 之五十九（B1+B2 前门接线）
> 已合入 dev=de0d5b6；B1/B2 从"仍未闭合"移入"已完成"，H14/H16 经亲核实为已完成（原快照误列为开）。

### 一、已完成（亲核 git+代码坐实，纠正过期标记）

| 项 | 原标记 | dev 真实状态 | 证据 |
|---|---|---|---|
| A1 SSO 反代替换 v2 | ⏳执行中(之五十二) | ✅ 已合 | HMAC v2(method/path/body/nonce)+nonce LRU+真LDAP链路，`8944a62`/`99fec2f` |
| A1 P4 nonce store | backlog | ✅ 已合 | NonceStore Protocol+InMemory+Redis fail-soft，之五十六 |
| A2 wsproxy+start.bat | 预留 | ✅ 已合 | wsproxy 真接LDAP+start.bat+部署文档，之五十七 |
| 5.3/5.4 P4 收口 | P4 backlog | ✅ 已合 | rate/token phase区分+决策⑫ e2e mock，之五十三 |
| ADR-0006 real-mode | — | ✅ 已落盘 | get_llm 默认真接(KYLIN_LLM_FAKE opt-in)，`5091a90` |
| H1 广谱 IDOR | 🔴High开 | ✅ 已闭合 | session_store owner字段+assert_owner+list_for过滤+admin bypass |
| H2 审计读越权 | 🟠High开 | ✅ 已闭合 | audit.py `_require_auditor_mode_aware` proxy模式{auditor,admin}否则403 |
| policy/* 暴露 | Medium开 | ✅ 已闭合 | policy.py `_operator_dep` {operator,auditor,admin}否则403 |
| SoD+actor 归因 | Medium开 | ✅ 已闭合 | approvals.py L-M3 actor==approver拒批+can_approve RBAC+审计记actor |
| H5 HMAC绑请求 | 🟠High开 | ✅ 已闭合 | A1.1 v2 canonical(7字段含method/path/body_sha/nonce) |
| B4 shutdown丢审计尾 | 🔴Blocker开 | ✅ 半闭合 | app.py:246-321 L-B4-2 顺序drain(registry→bus→audit flush→close) |
| v2 收尾 whoami/IDOR | D反馈P1 | ✅ 已合 | whoami 补4v2字段+去多余Depends避nonce自锁；principal_for_idor proxy真接verify_proxy_identity，之五十八 |
| **B1 前门认证接线** | 🔴Blocker开 | ✅ 已合 | nginx:443→proxy:8080→app:8000拓扑+剥8个X-Auth-*+TLS+安全头+限流；proxy sidecar systemd unit；install双单元，之五十九 |
| **B2 mock装错进程** | 🔴Blocker开 | ✅ 已合 | proxy.py模块import期fail-fast(KYLIN_LDAP_MOCK=true→SystemExit,ALLOW_MOCK opt-out)，之五十九 |
| H14 EventBus队列无界 | 🟠High开 | ✅ 已闭合 | event_bus.py EventBusQueueFull+maxsize(KYLIN_SSE_QUEUE_MAX)put_nowait满raise |
| H16 无集中日志 | 🟠High开 | ✅ 部分闭合 | main_logging.py dictConfig+JSON/console(KYLIN_LOG_MODE)；metrics仍缺 |

### 二、仍未闭合（阶段6 真实待办，按审计报告梯队）

| 项 | 梯队 | owner | dev 真实状态 | 缺口 |
|---|---|---|---|---|
| **B3 沙箱⊥NoNewPrivileges** | 一 | D | 🔴 开 | app 主单元 NoNewPrivileges 使 sudo 提权失败→开沙箱全工具失败；未组合验证（D VM 工单已派）|
| **B4 掉电丢trace尾** | 一 | L/D | 🟠 半开 | shutdown drain 已修；但 session.py 无 synchronous=FULL/busy_timeout/开机恢复（D DB durability 工单已派）|
| H3 空口令 LDAP bind 绕过 | 二 | X | 🟠 开 | ldap_client.py bind 无 `if not password` 防护 |
| H4 LDAP 明文无 TLS | 二 | X | 🟠 开 | ldap_client.py 无 use_ssl/Tls，service 账号口令明文过网 |
| H7 执行器超时不杀子进程 | 二 | D | 🟠 开 | privilege_executor 孤儿泄漏+输出全读进内存 DoS |
| H8 无 busy_timeout | 二 | D | 🟠 开 | VACUUM 锁库→live append 崩 |
| H9 summarize 输出侧收尾 | 二 | L | 🟠 窄开 | 注入扫描✅+输入净化`_sanitize_for_summary`✅+`GUARD_PROMPT`定界✅ 均已闭；仅剩输出侧`sensitive_filtered`死标志恒False（C4）|
| H10 审批页假成功 | 二 | X | 🟠 开 | 后端失败仍显"已通过" |
| H11 mock.ts 进生产 bundle | 二 | X | 🟠 开 | dist 主 chunk 含 mockSendMessage |
| H12 认证代码在 CI 外 | 二 | L | 🟠 开 | testpaths/mypy 不含 deploy/ → 认证 keystone 无守门 |
| H15 审计写阻塞 loop | 二 | L/D | 🟠 开 | orchestrator 同步 sqlite append 跑事件循环线程（H14 队列有界✅+H16 日志dictConfig✅ 已闭合，见上表）|
| SSE 并发连接上限 | 二 | L | 🟠 开 | EventBus 队列已有界(H14)，但并发 SSE 连接数无封顶 |
| metrics/prometheus 指标 | 二 | L | 🟠 开 | 全仓无指标系统（日志已集中，指标未建）|

**阶段6 启动主线进度**：第一梯队 B1+B2+B3+B4。**B1+B2（前门接线）✅ 已闭合**（之五十九，dev=de0d5b6：proxy fail-fast + kylin-proxy.service + nginx TLS/剥头/限流 + install 双单元）。**B3 沙箱 + B4 DB durability（session.py synchronous=FULL/busy_timeout）已派 D 域 VM 实证工单**。第一梯队仅剩 B3/B4 在 D 手上。

### 三、下一工单派发（留痕号由执行窗口起工单时 grep 真 max +1 分配，此处不预写字面号以免污染 grep）

- ✅ **已合 dev**：v2 收尾 bug 之五十八（whoami+principal_for_idor）、B1+B2 前门 之五十九
- ⏳ **D 域在跑**：B1+B2 VM 端到端实证 + B3 沙箱⊥NoNewPrivileges + B4 session.py durability（prompt 已派）
- ⏳ **X 域待办**：0cc0637（natural_language 守卫 state!==REJECTED）尚未 push origin，dev 前端仍带回归；H3/H4/H10/H11
- **L 域下一旗舰（本次派发）**：阶段6 二梯队 L 域可观测性加固集群 C1-C4（C1 metrics 指标 / C2 SSE 并发连接上限 / C3 H12 mypy 守门补 deploy/proxy / C4 H9 输出侧收尾）——纯 backend/app/{api,llm,agent}+deploy/proxy+CI，与 D 的 B3/B4（db/sandbox）零文件冲突（见对话窗口 prompt）
  - 订正：H9 经亲核**大部分已闭合**（`detect_tool_output_injection` 注入扫描 ✅ + `_sanitize_for_summary` 输入净化 ✅ + `GUARD_PROMPT` 定界 ✅ 均在 real_client.py/orchestrator.py），仅剩输出侧 `sensitive_filtered` 死标志恒 False（C4 收尾），**非整块待办**

### 四、当前阶段6/7 路线图进度全景（截至 dev=`6ef4ea4`，取代历史块内所有过期路线图表）

> 说明：下方 40+ 个 `★最新权威状态（之N）` 是逐时点**历史追加块**（只增不改，记录各时点真相），
> 其中"在跑工单/路线图摘要/⏳"均为**当时**状态，勿据历史块判断当前进度——**以本表为当前权威**。

| 阶段 | 项 | owner | 状态 | 依据 |
|---|---|---|---|---|
| 5 P4 | 5.3/5.4 rate/token phase + 决策⑫ e2e | L | ✅ 完成 | 之五十三 |
| 6 一梯队 | A1 SSO 反代 v2（HMAC+nonce+真LDAP） | L | ✅ 完成 | 之五十二/五十六 |
| 6 一梯队 | A2 wsproxy + start.bat | L | ✅ 完成 | 之五十七 |
| 6 一梯队 | v2 收尾（whoami+IDOR roles） | L | ✅ 完成 | 之五十八 |
| 6 一梯队 | **B1+B2 前门接线**（nginx443→proxy8080→app8000+mock fail-fast） | L | ✅ 完成 | 之五十九 |
| 6 一梯队 | **B3 沙箱⊥NoNewPrivileges** | D | ⏳ 在跑 | D VM 工单已派 |
| 6 一梯队 | **B4 session.py durability**（synchronous=FULL/busy_timeout/开机恢复） | D | ⏳ 在跑 | D 工单已派 |
| 6 一梯队 | B1+B2 VM 端到端实证 | D | ⏳ 在跑 | D 工单已派 |
| 6 二梯队 | **C1 metrics + C2 SSE封顶 + C3 mypy守门 + C4 H9输出收尾** | L | ⏳ 本次派发 | 对话 prompt |
| 6 二梯队 | H3 空口令bind / H4 LDAP TLS / H10 审批假成功 / H11 mock进bundle | X | 🟠 待起 | — |
| 6 二梯队 | H7 执行器超时杀进程 / H8 busy_timeout | D | 🟠 待起 | — |
| 6 二梯队 | H15 审计写阻塞 loop（to_thread）| L/D | ⏸ P2待调研 | Windows sqlite thread 问题 |
| 6 二梯队 | 0cc0637 前端 natural_language 守卫回归 | X | ⚠ 未push origin | dev 前端带回归 |
| 7 高级 | A4/B3'/D3/D5/E4/G3-G5/H/I4/J5/K3/L | 全员 | ⬜ 未启动 | 企业/合规级 |

> 本校准块为**状态索引校正**，不占 之X 交付号（把过期"预留/⏳"按 dev=de0d5b6 亲核归位）。

---

## ★最新权威状态（分支 `feat/l-v2-sign-fix-whoami-idor`，2026-07-15 之五十八 v2 签名收尾 2 bug 修复）

> 工单背景：D 完成 P1 反代全链路实证（真 client→proxy→app），顺带抓到 2 个 L 域 bug——
> 均为 v2 签名迁移（deps.py verify_token 已改 v2）时第二处调用点漏改所致，非安全过松。

- **之五十八** = v2 签名收尾 2 bug 修复（分支 `feat/l-v2-sign-fix-whoami-idor`，紧接工单3 之五十七）
  - **Bug1**（`routers/auth.py::whoami`）：proxy 模式下只传 4 个 v1 字段调 `verify_proxy_identity`，
    `_canonical` 走 v1(3 字段)分支，与反代签的 v2(7 字段)串不匹配 → 恒 401。修复：补 4 个 v2 字段
    （method/path/body_sha/nonce）。**修复时额外发现并规避**：whoami 原依赖
    `Depends(verify_token)` + 函数体自己再调一次 `verify_proxy_identity`——若同一 nonce 被
    verify_token 消费（record）一次，函数体第二次调用会撞见 `seen(nonce)==True` 误判重放 →
    自锁恒 401。修复为**去掉 `Depends(verify_token)`**，函数体那一次调用本身即 fail-closed
    认证闸门，不叠加。
  - **Bug2**（`deps.py::principal_for_idor`）：proxy 模式只从裸 `X-User-Role` 取 roles，但反代已
    剥除该头 → `principal.roles` 恒空 → IDOR `is_admin` 恒 False（过严）；审计 `actor.roles` 全空。
    修复：proxy 分支镜像 whoami/require_proxy_identity，调 `verify_proxy_identity` 拿含 roles 的
    完整 Principal（同样不叠加 `Depends(verify_token)`，理由同 Bug1）。
  - 2 守门用例（工单指定）：T1 proxy + 真 v2 签名头 → whoami 200 + roles 正确；T2 proxy +
    `X-Auth-Roles=admin` → `principal_for_idor.roles` 含 admin，且 `is_admin` **真从上一步
    `principal.roles` 派生**（非硬编码）验证 chat IDOR admin 可续他人 session。
  - pytest 亲跑：**666 passed, 17 skipped**（dev 基线 664 + 2 增量数学吻合）。

**C3 边界**：仅 `backend/app/api/{routers/auth.py, deps.py}` + `backend/tests/test_v2_sign_fix_whoami_idor.py`（新建）。未碰 proxy.py/前端/D 域/ADR/决策①-⑬/S3 哈希链。

---

## ★最新权威状态（分支 `feat/l-b1-b2-frontdoor-wiring`，2026-07-15 之五十九 B1+B2 前门接线）

> 阶段6 启动唯一卡启动 Blocker（架构审计报告 §2 B1+B2 + §6 第一梯队#1）。A1(HMAC v2)+A2(wsproxy 真接 LDAP)
> 已把密码学/LDAP 件备齐，本工单把它们接成生产拓扑：`nginx(443,TLS) → proxy sidecar(8080,真LDAP+HMAC) → app(8000)`。

**亲核现状（工单提示需重核，未照抄行号）**：
- `deploy/nginx.conf` 原 `listen 80` + 直连 `127.0.0.1:8000`（绕过 proxy sidecar，无 TLS，不剥客户端 X-Auth-*，无安全头）
- `deploy/proxy/proxy.py` 无 mock 守卫；ADR-0004 第三道保险落在 `backend/app/api/app.py:196`（app 进程从不实例化 LdapClient，守不到真跑 LDAP 的 proxy 进程）
- `deploy/install.sh` 只装 `kylin-safeops.service`（app 单元），无 proxy sidecar systemd unit

**4 commit（分支 `feat/l-b1-b2-frontdoor-wiring`）**：

| commit | 内容 | 文件 |
|---|---|---|
| `11fa54f` | B2：`proxy.py` 模块 import 期 `_fail_fast_if_mock_in_production()`（ADR-0004 第四道保险，落在真跑 LdapClient 的进程）；`KYLIN_PROXY_ALLOW_MOCK=true` 显式 opt-out（联调/CI 用）；3 用例子进程真跑验证 SystemExit(1) | `deploy/proxy/proxy.py` + 1 测试 |
| `7bea8bf` | B1a：新建 `deploy/proxy/kylin-proxy.service`（`--host 127.0.0.1 --port 8080`，`User=kylin-safeops` 非 root，`EnvironmentFile=/etc/kylin/proxy.env` 带外注入密钥，NoNewPrivileges/ProtectSystem 等加固）；3 用例 | `deploy/proxy/kylin-proxy.service` + 1 测试 |
| `78e9589` | B1b：`nginx.conf` 加固——`80→301 https`；`443 ssl`；upstream 改 `127.0.0.1:8080`（不再直连 app）；`proxy_set_header X-Auth-*/X-User-Role ""` 剥离客户端伪造头（纵深防御，proxy.py STRIP_HEADERS 之外再一层）；HSTS/X-Frame-Options/X-Content-Type-Options/CSP 安全头；`limit_req zone=kylin_api` 限流；5 用例 | `deploy/nginx.conf` + 1 测试 |
| `de64c10` | B1c：`install.sh` 装双单元（app + kylin-proxy）；建 `kylin-safeops` 系统用户（幂等 `id -u` 检查，`--shell /usr/sbin/nologin`）；写 `/etc/kylin/proxy.env` 0600 骨架（占位值，已存在则不覆盖防清空运维真值）；3 用例（真跑 `bash install.sh --dry-run`） | `deploy/install.sh` + 1 测试 |

**pytest 亲跑复核（各 commit 递增）**：669/17 → 672/17 → 677/17 → **680/17**（dev 基线 666 + 3+3+5+3=14 增量，666+14=680 数学吻合）。

**安全红线自检**：nginx TLS + 剥客户端 X-Auth-* + 限流三项齐全（工单硬性要求，缺一不许过）✅。

**C3 边界**：仅 `deploy/proxy/` + `deploy/nginx.conf` + `deploy/install.sh` + `deploy/proxy/tests/`。未碰 `backend/app`（除 B2 在 proxy.py 侧新增，app.py 侧原 fail-fast 保留做冗余、零改动）、前端、D 域 sandbox。

**域边界诚实标注**：本工单由 L 执行窗口做（延续 A1/A2 先例），审计报告 B1 owner 名义上写 X/L 共担；nginx 路由守卫/401 处理（§5 张力项）不在本工单，留 X 域单独起。

**已知风险 / VM 实证待办**：
1. `deploy/proxy/tests/` 新增 4 个测试文件均为本机/子进程级验证（unit 模板断言、nginx.conf 文本断言、install.sh --dry-run 输出断言），**非真实 systemd/nginx 集成测试**——真正的 `nginx:443→proxy:8080→app:8000` 全链路（含 TLS 握手、真 systemctl start 两单元、真 LDAP bind）需要 VM 实证，本机 Windows 无法验证。
2. TLS 证书路径在 nginx.conf 是占位（`/etc/nginx/ssl/kylin.{crt,key}`），VM 部署需运维生成/签发真证书。
3. `install.sh` 里 `cp -r deploy/proxy → deploy_proxy` 是否需要保留原 `deploy/proxy` 目录结构以支持 `uvicorn deploy.proxy.proxy:app` import 路径，VM 首次实机部署需验证 Python import 路径在 `/opt/kylin-safeops/` 下能否正确解析（本机未跑真实 pip install + uvoirn 启动）。
4. `limit_req_zone` 声明需要运维手动加进主 `nginx.conf` 的 `http{}` 块（本文件是 `server{}` 片段），已在文件顶部注释标注，VM 部署 checklist 需包含此步骤。

---

## ★最新权威状态（分支 `feat/l-stage6-t2-observability`，2026-07-15 之六十 C1-C4 可观测性+CI keystone+SSE封顶+H9收尾）

> 架构者阶段6 第二梯队旗舰工单：C1 metrics 指标系统 / C2 SSE 并发连接上限（H14 后半） /
> C3 mypy 守门补齐（H12 认证 keystone CI 外那半） / C4 H9 输出侧确定性凭据扫描收尾。
> dev 基线 `2aaa252`（=origin/dev）。真值以本次亲跑为准，起工单时已 grep 4 文档核验
> 真实 max = 之五十九（工单预判一致），本工单用 之六十。

**3 commit（分支 `feat/l-stage6-t2-observability`）**：

| commit | 内容 | 文件 |
|---|---|---|
| `860d4ce` | C1+C2：`backend/app/agent/metrics.py`（Metrics counter/gauge）+ orchestrator._goto/LLM plan/`_append_audit`/event_bus 4 处埋点 + `GET /api/system/metrics`；`chat.py::get_events` 加 `KYLIN_SSE_MAX_CONN` 上限守卫 | 8 files |
| `7e3f6bc` | C3：`.pre-commit-config.yaml` mypy files 正则加 `deploy/proxy`（exclude tests/）+ `pyproject.toml` `[[tool.mypy.overrides]] module="ldap3"` 消音第三方 stub 缺失 | 2 files |
| `9f3779a` | C4：`backend/app/agent/secret_scan.py::scan_and_redact`（与 audit_logger 同口径 6 类字段名正则）+ orchestrator `_emit_natural_language` 输出前扫描，`sensitive_filtered` 死标志变活 | 3 files |

**C1 埋点点位**：
- `orchestrator._goto`：`orchestrator.state.{state_name}` counter（每次合法状态转移 +1）
- `orchestrator.run` 主路径 + 观测回喂二次规划两处 LLM.plan 调用：`llm.calls` / `llm.failures` counter
- `orchestrator._append_audit`：`audit.append_latency_ms` gauge（毫秒，落库耗时）
- `event_bus.EventBus.create/remove/drain_all`：`sse.active_count` gauge（与既有 `active_count` property 同口径）

**C2**：`chat.py::get_events` 新连接前查 `bus.active_count >= KYLIN_SSE_MAX_CONN`（默认100）→
直接 503（不建队列），复用现有 active_count；释放（`bus.remove`）后 active_count 降，新连接可通过。

**C3 风险评估结果**：deploy/proxy 4 源文件（`__init__.py`/`_sign.py`/`proxy.py`/`wsproxy.py`）
唯一报错为 `deploy/sso/ldap_client.py:105` 传递性 import ldap3 缺 stub（该文件本身不在扫描范围内，
是 deploy/proxy 依赖它触发）——报错量极小（1个），直接用 per-module override 消音而非改类型标注，
不碰 X 域 `ldap_client.py` 源码。`deploy/app` 无 .py 文件（仅 systemd unit/env template/README），
未加入 mypy 范围。

**C4**：`scan_and_redact` 正则 `\b(api[_-]?key|authorization|bind[_-]?password|secret|token|password)\b\s*[:=]\s*\S+`
（大小写不敏感），命中即替换为 `<key>: ***REDACTED***`；orchestrator 调用点在 LLM.summarize 返回
非 None 之后、emit natural_language 事件之前，双重防线不冲突（S9 输入侧浅过滤 tool_results dict
字段 + 本次输出侧扫 LLM 生成的自由文本）。

pytest 亲跑复核（各 commit 递增）：685/17 → 685/17（C3 纯 config 无新用例）→ **689/17**（分支独立
基线 685 = dev 680 + 5（C1+C2 用例）；689 = 685 + 4（C4 用例），数学吻合 ✅）。

**C3 域边界**：仅 `backend/app/{agent,api}` + `.pre-commit-config.yaml` + `pyproject.toml` +
`backend/tests`。未碰 `backend/app/db`（D 正在动，本工单起工单前已核验避开）、`deploy/sandbox`、
frontend/、`deploy/sso`、契约6份、S3 哈希链、决策①-⑬、ADR。

**已知风险 / 未覆盖项**：
1. `limit_req_zone` 主配置声明缺口（之五十九已知项）与本工单无关，未处理。
2. metrics 无跨重启持久化（进程内单实例，与 EventBus/SessionRegistry/InMemoryNonceStore 同前提）。
3. `secret_scan.py` 纯正则字符串匹配，LLM 若把凭据拆行/变形描述可能漏检——已知局限，真正防线仍是
   S9 输入侧浅过滤（不让真实凭据进入 LLM 上下文），本次是纵深防御的第二层而非唯一防线。
4. 本工单未做真实 VM 验证（纯代码/CI 层收尾，拓扑与之五十九一致，无新增部署项）。

---
